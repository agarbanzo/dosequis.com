webpackJsonp([5],{117:function(module,exports){eval("// removed by extract-text-webpack-plugin\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scss/components/loader.scss\n// module id = 117\n// module chunks = 2 3 5\n\n//# sourceURL=webpack:///./dev/scss/components/loader.scss?")},118:function(module,exports,__webpack_require__){eval("var __WEBPACK_AMD_DEFINE_FACTORY__, __WEBPACK_AMD_DEFINE_RESULT__;(function(root, factory) {\n\n\tif (root === null) {\n\t\tthrow new Error('Google-maps package can be used only in browser');\n\t}\n\n\tif (true) {\n\t\t!(__WEBPACK_AMD_DEFINE_FACTORY__ = (factory),\n\t\t\t\t__WEBPACK_AMD_DEFINE_RESULT__ = (typeof __WEBPACK_AMD_DEFINE_FACTORY__ === 'function' ?\n\t\t\t\t(__WEBPACK_AMD_DEFINE_FACTORY__.call(exports, __webpack_require__, exports, module)) :\n\t\t\t\t__WEBPACK_AMD_DEFINE_FACTORY__),\n\t\t\t\t__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));\n\t} else if (typeof exports === 'object') {\n\t\tmodule.exports = factory();\n\t} else {\n\t\troot.GoogleMapsLoader = factory();\n\t}\n\n})(typeof window !== 'undefined' ? window : null, function() {\n\n\n\t'use strict';\n\n\n\tvar googleVersion = '3.18';\n\n\tvar script = null;\n\n\tvar google = null;\n\n\tvar loading = false;\n\n\tvar callbacks = [];\n\n\tvar onLoadEvents = [];\n\n\tvar originalCreateLoaderMethod = null;\n\n\n\tvar GoogleMapsLoader = {};\n\n\n\tGoogleMapsLoader.URL = 'https://maps.googleapis.com/maps/api/js';\n\n\tGoogleMapsLoader.KEY = null;\n\n\tGoogleMapsLoader.LIBRARIES = [];\n\n\tGoogleMapsLoader.CLIENT = null;\n\n\tGoogleMapsLoader.CHANNEL = null;\n\n\tGoogleMapsLoader.LANGUAGE = null;\n\n\tGoogleMapsLoader.REGION = null;\n\n\tGoogleMapsLoader.VERSION = googleVersion;\n\n\tGoogleMapsLoader.WINDOW_CALLBACK_NAME = '__google_maps_api_provider_initializator__';\n\n\n\tGoogleMapsLoader._googleMockApiObject = {};\n\n\n\tGoogleMapsLoader.load = function(fn) {\n\t\tif (google === null) {\n\t\t\tif (loading === true) {\n\t\t\t\tif (fn) {\n\t\t\t\t\tcallbacks.push(fn);\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\tloading = true;\n\n\t\t\t\twindow[GoogleMapsLoader.WINDOW_CALLBACK_NAME] = function() {\n\t\t\t\t\tready(fn);\n\t\t\t\t};\n\n\t\t\t\tGoogleMapsLoader.createLoader();\n\t\t\t}\n\t\t} else if (fn) {\n\t\t\tfn(google);\n\t\t}\n\t};\n\n\n\tGoogleMapsLoader.createLoader = function() {\n\t\tscript = document.createElement('script');\n\t\tscript.type = 'text/javascript';\n\t\tscript.src = GoogleMapsLoader.createUrl();\n\n\t\tdocument.body.appendChild(script);\n\t};\n\n\n\tGoogleMapsLoader.isLoaded = function() {\n\t\treturn google !== null;\n\t};\n\n\n\tGoogleMapsLoader.createUrl = function() {\n\t\tvar url = GoogleMapsLoader.URL;\n\n\t\turl += '?callback=' + GoogleMapsLoader.WINDOW_CALLBACK_NAME;\n\n\t\tif (GoogleMapsLoader.KEY) {\n\t\t\turl += '&key=' + GoogleMapsLoader.KEY;\n\t\t}\n\n\t\tif (GoogleMapsLoader.LIBRARIES.length > 0) {\n\t\t\turl += '&libraries=' + GoogleMapsLoader.LIBRARIES.join(',');\n\t\t}\n\n\t\tif (GoogleMapsLoader.CLIENT) {\n\t\t\turl += '&client=' + GoogleMapsLoader.CLIENT + '&v=' + GoogleMapsLoader.VERSION;\n\t\t}\n\n\t\tif (GoogleMapsLoader.CHANNEL) {\n\t\t\turl += '&channel=' + GoogleMapsLoader.CHANNEL;\n\t\t}\n\n\t\tif (GoogleMapsLoader.LANGUAGE) {\n\t\t\turl += '&language=' + GoogleMapsLoader.LANGUAGE;\n\t\t}\n\n\t\tif (GoogleMapsLoader.REGION) {\n\t\t\turl += '&region=' + GoogleMapsLoader.REGION;\n\t\t}\n\n\t\treturn url;\n\t};\n\n\n\tGoogleMapsLoader.release = function(fn) {\n\t\tvar release = function() {\n\t\t\tGoogleMapsLoader.KEY = null;\n\t\t\tGoogleMapsLoader.LIBRARIES = [];\n\t\t\tGoogleMapsLoader.CLIENT = null;\n\t\t\tGoogleMapsLoader.CHANNEL = null;\n\t\t\tGoogleMapsLoader.LANGUAGE = null;\n\t\t\tGoogleMapsLoader.REGION = null;\n\t\t\tGoogleMapsLoader.VERSION = googleVersion;\n\n\t\t\tgoogle = null;\n\t\t\tloading = false;\n\t\t\tcallbacks = [];\n\t\t\tonLoadEvents = [];\n\n\t\t\tif (typeof window.google !== 'undefined') {\n\t\t\t\tdelete window.google;\n\t\t\t}\n\n\t\t\tif (typeof window[GoogleMapsLoader.WINDOW_CALLBACK_NAME] !== 'undefined') {\n\t\t\t\tdelete window[GoogleMapsLoader.WINDOW_CALLBACK_NAME];\n\t\t\t}\n\n\t\t\tif (originalCreateLoaderMethod !== null) {\n\t\t\t\tGoogleMapsLoader.createLoader = originalCreateLoaderMethod;\n\t\t\t\toriginalCreateLoaderMethod = null;\n\t\t\t}\n\n\t\t\tif (script !== null) {\n\t\t\t\tscript.parentElement.removeChild(script);\n\t\t\t\tscript = null;\n\t\t\t}\n\n\t\t\tif (fn) {\n\t\t\t\tfn();\n\t\t\t}\n\t\t};\n\n\t\tif (loading) {\n\t\t\tGoogleMapsLoader.load(function() {\n\t\t\t\trelease();\n\t\t\t});\n\t\t} else {\n\t\t\trelease();\n\t\t}\n\t};\n\n\n\tGoogleMapsLoader.onLoad = function(fn) {\n\t\tonLoadEvents.push(fn);\n\t};\n\n\n\tGoogleMapsLoader.makeMock = function() {\n\t\toriginalCreateLoaderMethod = GoogleMapsLoader.createLoader;\n\n\t\tGoogleMapsLoader.createLoader = function() {\n\t\t\twindow.google = GoogleMapsLoader._googleMockApiObject;\n\t\t\twindow[GoogleMapsLoader.WINDOW_CALLBACK_NAME]();\n\t\t};\n\t};\n\n\n\tvar ready = function(fn) {\n\t\tvar i;\n\n\t\tloading = false;\n\n\t\tif (google === null) {\n\t\t\tgoogle = window.google;\n\t\t}\n\n\t\tfor (i = 0; i < onLoadEvents.length; i++) {\n\t\t\tonLoadEvents[i](google);\n\t\t}\n\n\t\tif (fn) {\n\t\t\tfn(google);\n\t\t}\n\n\t\tfor (i = 0; i < callbacks.length; i++) {\n\t\t\tcallbacks[i](google);\n\t\t}\n\n\t\tcallbacks = [];\n\t};\n\n\n\treturn GoogleMapsLoader;\n\n});\n\n\n//////////////////\n// WEBPACK FOOTER\n// ./~/google-maps/lib/Google.js\n// module id = 118\n// module chunks = 3 5\n\n//# sourceURL=webpack:///./~/google-maps/lib/Google.js?")},126:function(module,exports,__webpack_require__){eval('var H = __webpack_require__(27);\nmodule.exports = function() { var T = new H.Template({code: function (c,p,i) { var t=this;t.b(i=i||"");t.b("<div>\\r");t.b("\\n" + i);t.b("  <div></div>\\r");t.b("\\n" + i);t.b("  <div></div>\\r");t.b("\\n" + i);t.b("  <div></div>\\r");t.b("\\n" + i);t.b("  <div></div>\\r");t.b("\\n" + i);t.b("</div>\\r");t.b("\\n" + i);t.b("<div><img src=\\"");t.b(t.v(t.f("img",c,p,0)));t.b("\\"/></div>");return t.fl(); },partials: {}, subs: {  }}, "<div>\\r\\n  <div></div>\\r\\n  <div></div>\\r\\n  <div></div>\\r\\n  <div></div>\\r\\n</div>\\r\\n<div><img src=\\"{{img}}\\"/></div>", H);return T.render.apply(T, arguments); };\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/components/loader/loader.html\n// module id = 126\n// module chunks = 2 3 5\n\n//# sourceURL=webpack:///./dev/scripts/components/loader/loader.html?')},127:function(module,exports,__webpack_require__){eval('module.exports = __webpack_require__.p + "img/img-fdcc3a.png";\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/components/loader/logo-white.png\n// module id = 127\n// module chunks = 2 3 5\n\n//# sourceURL=webpack:///./dev/scripts/components/loader/logo-white.png?')},158:function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\nexports.googleApiKey = googleApiKey;\nfunction googleApiKey() {\n  var key = 'AIzaSyC-WdmV7zVu5qELVpEAxQ35u0zUF3e_ceg';\n  return key;\n}\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/utils/google_api.js\n// module id = 158\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/utils/google_api.js?")},278:function(module,exports,__webpack_require__){"use strict";eval("/* WEBPACK VAR INJECTION */(function($) {\n\nvar _googleMaps = __webpack_require__(118);\n\nvar _googleMaps2 = _interopRequireDefault(_googleMaps);\n\nvar _google_api = __webpack_require__(158);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nvar _Promise2 = typeof Promise === 'undefined' ? __webpack_require__(310).Promise : Promise;\n\nvar _Promise = typeof _Promise2 === 'undefined' ? __webpack_require__(310).Promise : _Promise2;\n\nmodule.exports = {\n\tapiKEY: (0, _google_api.googleApiKey)(),\n\tinit: function init() {\n\t\t_googleMaps2.default.KEY = this.apiKEY;\n\t\t_googleMaps2.default.LIBRARIES = ['places'];\n\t},\n\tgeoLocate: function geoLocate() {\n\t\tvar _this = this;\n\n\t\treturn new _Promise(function (resolve, reject) {\n\t\t\t$.ajax({\n\t\t\t\tmethod: \"POST\",\n\t\t\t\taccept: \"application/json\",\n\t\t\t\tcontentType: \"application/json; charset=utf-8\",\n\t\t\t\tcrossDomain: true,\n\t\t\t\tdataType: \"json\",\n\t\t\t\turl: 'https://www.googleapis.com/geolocation/v1/geolocate?key=' + _this.apiKEY,\n\t\t\t\tsuccess: function success(_success, status) {\n\t\t\t\t\tvar loc = {\n\t\t\t\t\t\tlat: _success.location.lat,\n\t\t\t\t\t\tlng: _success.location.lng\n\t\t\t\t\t};\n\t\t\t\t\tresolve(loc);\n\t\t\t\t},\n\t\t\t\terror: function error(err) {\n\t\t\t\t\tresolve({\n\t\t\t\t\t\terror: err,\n\t\t\t\t\t\tmsg: \"API Geolocation error!\"\n\t\t\t\t\t});\n\t\t\t\t}\n\t\t\t});\n\t\t});\n\t},\n\tgetLatLng: function getLatLng(address) {\n\t\treturn new _Promise(function (resolve, reject) {\n\t\t\tvar latlng;\n\t\t\tvar geocoder = new google.maps.Geocoder();\n\t\t\tgeocoder.geocode({\n\t\t\t\t'address': address\n\t\t\t}, function (results, status) {\n\t\t\t\tif (status == google.maps.GeocoderStatus.OK) {\n\t\t\t\t\tvar latitude = results[0].geometry.location.lat();\n\t\t\t\t\tvar longitude = results[0].geometry.location.lng();\n\t\t\t\t\tvar latlng = {\n\t\t\t\t\t\tlat: latitude,\n\t\t\t\t\t\tlng: longitude\n\t\t\t\t\t};\n\t\t\t\t\t//console.log('my loc is ', latlng)\n\t\t\t\t\twindow.dispatcher.trigger('onLatLngChanged', [results[0]]);\n\t\t\t\t\tresolve(latlng);\n\t\t\t\t} else {}\n\t\t\t});\n\t\t});\n\t},\n\tgetZip: function getZip(position) {\n\t\t// reverse geolocation to get zip\n\t\treturn new _Promise(function (resolve, reject) {\n\t\t\tvar zipcode;\n\t\t\t_googleMaps2.default.load(function (google) {\n\t\t\t\tvar geocoder = new google.maps.Geocoder();\n\t\t\t\tvar latlng = {\n\t\t\t\t\tlat: position.lat,\n\t\t\t\t\tlng: position.lng\n\t\t\t\t};\n\t\t\t\tgeocoder.geocode({\n\t\t\t\t\t'location': position\n\t\t\t\t}, function (results, status) {\n\t\t\t\t\t//console.log('geocoder status=', status);\n\t\t\t\t\tif (status !== 'OK') {\n\t\t\t\t\t\treject('error:' + status);\n\t\t\t\t\t} else {\n\t\t\t\t\t\tfor (var i = 0; i < results.length; i++) {\n\t\t\t\t\t\t\tfor (var j = 0; j < results[i].address_components.length; j++) {\n\t\t\t\t\t\t\t\tfor (var k = 0; k < results[i].address_components[j].types.length; k++) {\n\t\t\t\t\t\t\t\t\tif (results[i].address_components[j].types[k] == \"postal_code\") {\n\t\t\t\t\t\t\t\t\t\tzipcode = results[i].address_components[j].short_name;\n\t\t\t\t\t\t\t\t\t\tresolve(zipcode);\n\t\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t}\n\t\t\t\t\t}\n\t\t\t\t});\n\t\t\t}, function (error) {\n\t\t\t\treject('error ==', error);\n\t\t\t});\n\t\t}).catch(function (error) {\n\t\t\treturn error;\n\t\t});\n\t},\n\tgetAddress: function getAddress(position) {\n\t\t// reverse geolocation to get zip\n\t\treturn new _Promise(function (resolve, reject) {\n\t\t\tvar zipcode;\n\t\t\t_googleMaps2.default.load(function (google) {\n\t\t\t\tvar geocoder = new google.maps.Geocoder();\n\t\t\t\tvar latlng = {\n\t\t\t\t\tlat: position.lat,\n\t\t\t\t\tlng: position.lng\n\t\t\t\t};\n\t\t\t\tgeocoder.geocode({\n\t\t\t\t\t'location': position\n\t\t\t\t}, function (results, status) {\n\t\t\t\t\t//console.log('geocoder status=', status);\n\t\t\t\t\tif (status !== 'OK') {\n\t\t\t\t\t\treject('error:' + status);\n\t\t\t\t\t} else {\n\t\t\t\t\t\tresolve(results[0].formatted_address);\n\t\t\t\t\t}\n\t\t\t\t});\n\t\t\t}, function (error) {\n\t\t\t\treject('error ==', error);\n\t\t\t});\n\t\t}).catch(function (error) {\n\t\t\treturn error;\n\t\t});\n\t},\n\tautoComplete: function autoComplete(el) {\n\t\tvar autocomplete;\n\t\tvar input = el[0];\n\t\tvar options = {\n\t\t\ttypes: ['geocode'],\n\t\t\tcomponentRestrictions: { country: \"us\" }\n\t\t};\n\t\t_googleMaps2.default.load(function (google) {\n\t\t\tvar input = el;\n\t\t\tvar autocomplete = new google.maps.places.Autocomplete(input, options);\n\t\t\tautocomplete.addListener('place_changed', function () {\n\t\t\t\tvar place = autocomplete.getPlace();\n\t\t\t\tif (!place.geometry) {\n\t\t\t\t\treturn;\n\t\t\t\t}\n\t\t\t\tif (place.geometry.viewport) {\n\t\t\t\t\twindow.dispatcher.trigger('onGPlacesChanged', [place.geometry.location, place.types[0]]);\n\t\t\t\t}\n\t\t\t});\n\t\t});\n\t},\n\tgetFirstSuggestion: function getFirstSuggestion(input) {\n\t\tvar _addEventListener = input.addEventListener ? input.addEventListener : input.attachEvent;\n\t\tfunction addEventListenerWrapper(type, listener) {\n\t\t\t// Simulate a 'down arrow' keypress on hitting 'return' when no pac suggestion is selected,\n\t\t\t// and then trigger the original listener.\n\t\t\tif (type == \"keydown\") {\n\t\t\t\tvar orig_listener = listener;\n\t\t\t\tlistener = function listener(event) {\n\t\t\t\t\tvar suggestion_selected = $(\".pac-item-selected\").length > 0;\n\t\t\t\t\tif (event.which == 13 && !suggestion_selected) {\n\t\t\t\t\t\tvar simulated_downarrow = $.Event(\"keydown\", { keyCode: 40, which: 40 });\n\t\t\t\t\t\twindow.dispatcher.trigger('onFirstSuggestion');\n\t\t\t\t\t\torig_listener.apply(input, [simulated_downarrow]);\n\t\t\t\t\t}\n\t\t\t\t\torig_listener.apply(input, [event]);\n\t\t\t\t};\n\t\t\t}\n\t\t\t// add the modified listener\n\t\t\t_addEventListener.apply(input, [type, listener]);\n\t\t}\n\t\tif (input.addEventListener) input.addEventListener = addEventListenerWrapper;else if (input.attachEvent) input.attachEvent = addEventListenerWrapper;\n\t}\n};\n/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(1)))\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/utils/gplaces.js\n// module id = 278\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/utils/gplaces.js?")},310:function(module,exports,__webpack_require__){eval("/* WEBPACK VAR INJECTION */(function(process, global) {var require;/*!\n * @overview es6-promise - a tiny implementation of Promises/A+.\n * @copyright Copyright (c) 2014 Yehuda Katz, Tom Dale, Stefan Penner and contributors (Conversion to ES6 API by Jake Archibald)\n * @license   Licensed under MIT license\n *            See https://raw.githubusercontent.com/stefanpenner/es6-promise/master/LICENSE\n * @version   4.1.0\n */\n\n(function (global, factory) {\n     true ? module.exports = factory() :\n    typeof define === 'function' && define.amd ? define(factory) :\n    (global.ES6Promise = factory());\n}(this, (function () { 'use strict';\n\nfunction objectOrFunction(x) {\n  return typeof x === 'function' || typeof x === 'object' && x !== null;\n}\n\nfunction isFunction(x) {\n  return typeof x === 'function';\n}\n\nvar _isArray = undefined;\nif (!Array.isArray) {\n  _isArray = function (x) {\n    return Object.prototype.toString.call(x) === '[object Array]';\n  };\n} else {\n  _isArray = Array.isArray;\n}\n\nvar isArray = _isArray;\n\nvar len = 0;\nvar vertxNext = undefined;\nvar customSchedulerFn = undefined;\n\nvar asap = function asap(callback, arg) {\n  queue[len] = callback;\n  queue[len + 1] = arg;\n  len += 2;\n  if (len === 2) {\n    // If len is 2, that means that we need to schedule an async flush.\n    // If additional callbacks are queued before the queue is flushed, they\n    // will be processed by this flush that we are scheduling.\n    if (customSchedulerFn) {\n      customSchedulerFn(flush);\n    } else {\n      scheduleFlush();\n    }\n  }\n};\n\nfunction setScheduler(scheduleFn) {\n  customSchedulerFn = scheduleFn;\n}\n\nfunction setAsap(asapFn) {\n  asap = asapFn;\n}\n\nvar browserWindow = typeof window !== 'undefined' ? window : undefined;\nvar browserGlobal = browserWindow || {};\nvar BrowserMutationObserver = browserGlobal.MutationObserver || browserGlobal.WebKitMutationObserver;\nvar isNode = typeof self === 'undefined' && typeof process !== 'undefined' && ({}).toString.call(process) === '[object process]';\n\n// test for web worker but not in IE10\nvar isWorker = typeof Uint8ClampedArray !== 'undefined' && typeof importScripts !== 'undefined' && typeof MessageChannel !== 'undefined';\n\n// node\nfunction useNextTick() {\n  // node version 0.10.x displays a deprecation warning when nextTick is used recursively\n  // see https://github.com/cujojs/when/issues/410 for details\n  return function () {\n    return process.nextTick(flush);\n  };\n}\n\n// vertx\nfunction useVertxTimer() {\n  if (typeof vertxNext !== 'undefined') {\n    return function () {\n      vertxNext(flush);\n    };\n  }\n\n  return useSetTimeout();\n}\n\nfunction useMutationObserver() {\n  var iterations = 0;\n  var observer = new BrowserMutationObserver(flush);\n  var node = document.createTextNode('');\n  observer.observe(node, { characterData: true });\n\n  return function () {\n    node.data = iterations = ++iterations % 2;\n  };\n}\n\n// web worker\nfunction useMessageChannel() {\n  var channel = new MessageChannel();\n  channel.port1.onmessage = flush;\n  return function () {\n    return channel.port2.postMessage(0);\n  };\n}\n\nfunction useSetTimeout() {\n  // Store setTimeout reference so es6-promise will be unaffected by\n  // other code modifying setTimeout (like sinon.useFakeTimers())\n  var globalSetTimeout = setTimeout;\n  return function () {\n    return globalSetTimeout(flush, 1);\n  };\n}\n\nvar queue = new Array(1000);\nfunction flush() {\n  for (var i = 0; i < len; i += 2) {\n    var callback = queue[i];\n    var arg = queue[i + 1];\n\n    callback(arg);\n\n    queue[i] = undefined;\n    queue[i + 1] = undefined;\n  }\n\n  len = 0;\n}\n\nfunction attemptVertx() {\n  try {\n    var r = require;\n    var vertx = __webpack_require__(578);\n    vertxNext = vertx.runOnLoop || vertx.runOnContext;\n    return useVertxTimer();\n  } catch (e) {\n    return useSetTimeout();\n  }\n}\n\nvar scheduleFlush = undefined;\n// Decide what async method to use to triggering processing of queued callbacks:\nif (isNode) {\n  scheduleFlush = useNextTick();\n} else if (BrowserMutationObserver) {\n  scheduleFlush = useMutationObserver();\n} else if (isWorker) {\n  scheduleFlush = useMessageChannel();\n} else if (browserWindow === undefined && \"function\" === 'function') {\n  scheduleFlush = attemptVertx();\n} else {\n  scheduleFlush = useSetTimeout();\n}\n\nfunction then(onFulfillment, onRejection) {\n  var _arguments = arguments;\n\n  var parent = this;\n\n  var child = new this.constructor(noop);\n\n  if (child[PROMISE_ID] === undefined) {\n    makePromise(child);\n  }\n\n  var _state = parent._state;\n\n  if (_state) {\n    (function () {\n      var callback = _arguments[_state - 1];\n      asap(function () {\n        return invokeCallback(_state, child, callback, parent._result);\n      });\n    })();\n  } else {\n    subscribe(parent, child, onFulfillment, onRejection);\n  }\n\n  return child;\n}\n\n/**\n  `Promise.resolve` returns a promise that will become resolved with the\n  passed `value`. It is shorthand for the following:\n\n  ```javascript\n  let promise = new Promise(function(resolve, reject){\n    resolve(1);\n  });\n\n  promise.then(function(value){\n    // value === 1\n  });\n  ```\n\n  Instead of writing the above, your code now simply becomes the following:\n\n  ```javascript\n  let promise = Promise.resolve(1);\n\n  promise.then(function(value){\n    // value === 1\n  });\n  ```\n\n  @method resolve\n  @static\n  @param {Any} value value that the returned promise will be resolved with\n  Useful for tooling.\n  @return {Promise} a promise that will become fulfilled with the given\n  `value`\n*/\nfunction resolve(object) {\n  /*jshint validthis:true */\n  var Constructor = this;\n\n  if (object && typeof object === 'object' && object.constructor === Constructor) {\n    return object;\n  }\n\n  var promise = new Constructor(noop);\n  _resolve(promise, object);\n  return promise;\n}\n\nvar PROMISE_ID = Math.random().toString(36).substring(16);\n\nfunction noop() {}\n\nvar PENDING = void 0;\nvar FULFILLED = 1;\nvar REJECTED = 2;\n\nvar GET_THEN_ERROR = new ErrorObject();\n\nfunction selfFulfillment() {\n  return new TypeError(\"You cannot resolve a promise with itself\");\n}\n\nfunction cannotReturnOwn() {\n  return new TypeError('A promises callback cannot return that same promise.');\n}\n\nfunction getThen(promise) {\n  try {\n    return promise.then;\n  } catch (error) {\n    GET_THEN_ERROR.error = error;\n    return GET_THEN_ERROR;\n  }\n}\n\nfunction tryThen(then, value, fulfillmentHandler, rejectionHandler) {\n  try {\n    then.call(value, fulfillmentHandler, rejectionHandler);\n  } catch (e) {\n    return e;\n  }\n}\n\nfunction handleForeignThenable(promise, thenable, then) {\n  asap(function (promise) {\n    var sealed = false;\n    var error = tryThen(then, thenable, function (value) {\n      if (sealed) {\n        return;\n      }\n      sealed = true;\n      if (thenable !== value) {\n        _resolve(promise, value);\n      } else {\n        fulfill(promise, value);\n      }\n    }, function (reason) {\n      if (sealed) {\n        return;\n      }\n      sealed = true;\n\n      _reject(promise, reason);\n    }, 'Settle: ' + (promise._label || ' unknown promise'));\n\n    if (!sealed && error) {\n      sealed = true;\n      _reject(promise, error);\n    }\n  }, promise);\n}\n\nfunction handleOwnThenable(promise, thenable) {\n  if (thenable._state === FULFILLED) {\n    fulfill(promise, thenable._result);\n  } else if (thenable._state === REJECTED) {\n    _reject(promise, thenable._result);\n  } else {\n    subscribe(thenable, undefined, function (value) {\n      return _resolve(promise, value);\n    }, function (reason) {\n      return _reject(promise, reason);\n    });\n  }\n}\n\nfunction handleMaybeThenable(promise, maybeThenable, then$$) {\n  if (maybeThenable.constructor === promise.constructor && then$$ === then && maybeThenable.constructor.resolve === resolve) {\n    handleOwnThenable(promise, maybeThenable);\n  } else {\n    if (then$$ === GET_THEN_ERROR) {\n      _reject(promise, GET_THEN_ERROR.error);\n      GET_THEN_ERROR.error = null;\n    } else if (then$$ === undefined) {\n      fulfill(promise, maybeThenable);\n    } else if (isFunction(then$$)) {\n      handleForeignThenable(promise, maybeThenable, then$$);\n    } else {\n      fulfill(promise, maybeThenable);\n    }\n  }\n}\n\nfunction _resolve(promise, value) {\n  if (promise === value) {\n    _reject(promise, selfFulfillment());\n  } else if (objectOrFunction(value)) {\n    handleMaybeThenable(promise, value, getThen(value));\n  } else {\n    fulfill(promise, value);\n  }\n}\n\nfunction publishRejection(promise) {\n  if (promise._onerror) {\n    promise._onerror(promise._result);\n  }\n\n  publish(promise);\n}\n\nfunction fulfill(promise, value) {\n  if (promise._state !== PENDING) {\n    return;\n  }\n\n  promise._result = value;\n  promise._state = FULFILLED;\n\n  if (promise._subscribers.length !== 0) {\n    asap(publish, promise);\n  }\n}\n\nfunction _reject(promise, reason) {\n  if (promise._state !== PENDING) {\n    return;\n  }\n  promise._state = REJECTED;\n  promise._result = reason;\n\n  asap(publishRejection, promise);\n}\n\nfunction subscribe(parent, child, onFulfillment, onRejection) {\n  var _subscribers = parent._subscribers;\n  var length = _subscribers.length;\n\n  parent._onerror = null;\n\n  _subscribers[length] = child;\n  _subscribers[length + FULFILLED] = onFulfillment;\n  _subscribers[length + REJECTED] = onRejection;\n\n  if (length === 0 && parent._state) {\n    asap(publish, parent);\n  }\n}\n\nfunction publish(promise) {\n  var subscribers = promise._subscribers;\n  var settled = promise._state;\n\n  if (subscribers.length === 0) {\n    return;\n  }\n\n  var child = undefined,\n      callback = undefined,\n      detail = promise._result;\n\n  for (var i = 0; i < subscribers.length; i += 3) {\n    child = subscribers[i];\n    callback = subscribers[i + settled];\n\n    if (child) {\n      invokeCallback(settled, child, callback, detail);\n    } else {\n      callback(detail);\n    }\n  }\n\n  promise._subscribers.length = 0;\n}\n\nfunction ErrorObject() {\n  this.error = null;\n}\n\nvar TRY_CATCH_ERROR = new ErrorObject();\n\nfunction tryCatch(callback, detail) {\n  try {\n    return callback(detail);\n  } catch (e) {\n    TRY_CATCH_ERROR.error = e;\n    return TRY_CATCH_ERROR;\n  }\n}\n\nfunction invokeCallback(settled, promise, callback, detail) {\n  var hasCallback = isFunction(callback),\n      value = undefined,\n      error = undefined,\n      succeeded = undefined,\n      failed = undefined;\n\n  if (hasCallback) {\n    value = tryCatch(callback, detail);\n\n    if (value === TRY_CATCH_ERROR) {\n      failed = true;\n      error = value.error;\n      value.error = null;\n    } else {\n      succeeded = true;\n    }\n\n    if (promise === value) {\n      _reject(promise, cannotReturnOwn());\n      return;\n    }\n  } else {\n    value = detail;\n    succeeded = true;\n  }\n\n  if (promise._state !== PENDING) {\n    // noop\n  } else if (hasCallback && succeeded) {\n      _resolve(promise, value);\n    } else if (failed) {\n      _reject(promise, error);\n    } else if (settled === FULFILLED) {\n      fulfill(promise, value);\n    } else if (settled === REJECTED) {\n      _reject(promise, value);\n    }\n}\n\nfunction initializePromise(promise, resolver) {\n  try {\n    resolver(function resolvePromise(value) {\n      _resolve(promise, value);\n    }, function rejectPromise(reason) {\n      _reject(promise, reason);\n    });\n  } catch (e) {\n    _reject(promise, e);\n  }\n}\n\nvar id = 0;\nfunction nextId() {\n  return id++;\n}\n\nfunction makePromise(promise) {\n  promise[PROMISE_ID] = id++;\n  promise._state = undefined;\n  promise._result = undefined;\n  promise._subscribers = [];\n}\n\nfunction Enumerator(Constructor, input) {\n  this._instanceConstructor = Constructor;\n  this.promise = new Constructor(noop);\n\n  if (!this.promise[PROMISE_ID]) {\n    makePromise(this.promise);\n  }\n\n  if (isArray(input)) {\n    this._input = input;\n    this.length = input.length;\n    this._remaining = input.length;\n\n    this._result = new Array(this.length);\n\n    if (this.length === 0) {\n      fulfill(this.promise, this._result);\n    } else {\n      this.length = this.length || 0;\n      this._enumerate();\n      if (this._remaining === 0) {\n        fulfill(this.promise, this._result);\n      }\n    }\n  } else {\n    _reject(this.promise, validationError());\n  }\n}\n\nfunction validationError() {\n  return new Error('Array Methods must be provided an Array');\n};\n\nEnumerator.prototype._enumerate = function () {\n  var length = this.length;\n  var _input = this._input;\n\n  for (var i = 0; this._state === PENDING && i < length; i++) {\n    this._eachEntry(_input[i], i);\n  }\n};\n\nEnumerator.prototype._eachEntry = function (entry, i) {\n  var c = this._instanceConstructor;\n  var resolve$$ = c.resolve;\n\n  if (resolve$$ === resolve) {\n    var _then = getThen(entry);\n\n    if (_then === then && entry._state !== PENDING) {\n      this._settledAt(entry._state, i, entry._result);\n    } else if (typeof _then !== 'function') {\n      this._remaining--;\n      this._result[i] = entry;\n    } else if (c === Promise) {\n      var promise = new c(noop);\n      handleMaybeThenable(promise, entry, _then);\n      this._willSettleAt(promise, i);\n    } else {\n      this._willSettleAt(new c(function (resolve$$) {\n        return resolve$$(entry);\n      }), i);\n    }\n  } else {\n    this._willSettleAt(resolve$$(entry), i);\n  }\n};\n\nEnumerator.prototype._settledAt = function (state, i, value) {\n  var promise = this.promise;\n\n  if (promise._state === PENDING) {\n    this._remaining--;\n\n    if (state === REJECTED) {\n      _reject(promise, value);\n    } else {\n      this._result[i] = value;\n    }\n  }\n\n  if (this._remaining === 0) {\n    fulfill(promise, this._result);\n  }\n};\n\nEnumerator.prototype._willSettleAt = function (promise, i) {\n  var enumerator = this;\n\n  subscribe(promise, undefined, function (value) {\n    return enumerator._settledAt(FULFILLED, i, value);\n  }, function (reason) {\n    return enumerator._settledAt(REJECTED, i, reason);\n  });\n};\n\n/**\n  `Promise.all` accepts an array of promises, and returns a new promise which\n  is fulfilled with an array of fulfillment values for the passed promises, or\n  rejected with the reason of the first passed promise to be rejected. It casts all\n  elements of the passed iterable to promises as it runs this algorithm.\n\n  Example:\n\n  ```javascript\n  let promise1 = resolve(1);\n  let promise2 = resolve(2);\n  let promise3 = resolve(3);\n  let promises = [ promise1, promise2, promise3 ];\n\n  Promise.all(promises).then(function(array){\n    // The array here would be [ 1, 2, 3 ];\n  });\n  ```\n\n  If any of the `promises` given to `all` are rejected, the first promise\n  that is rejected will be given as an argument to the returned promises's\n  rejection handler. For example:\n\n  Example:\n\n  ```javascript\n  let promise1 = resolve(1);\n  let promise2 = reject(new Error(\"2\"));\n  let promise3 = reject(new Error(\"3\"));\n  let promises = [ promise1, promise2, promise3 ];\n\n  Promise.all(promises).then(function(array){\n    // Code here never runs because there are rejected promises!\n  }, function(error) {\n    // error.message === \"2\"\n  });\n  ```\n\n  @method all\n  @static\n  @param {Array} entries array of promises\n  @param {String} label optional string for labeling the promise.\n  Useful for tooling.\n  @return {Promise} promise that is fulfilled when all `promises` have been\n  fulfilled, or rejected if any of them become rejected.\n  @static\n*/\nfunction all(entries) {\n  return new Enumerator(this, entries).promise;\n}\n\n/**\n  `Promise.race` returns a new promise which is settled in the same way as the\n  first passed promise to settle.\n\n  Example:\n\n  ```javascript\n  let promise1 = new Promise(function(resolve, reject){\n    setTimeout(function(){\n      resolve('promise 1');\n    }, 200);\n  });\n\n  let promise2 = new Promise(function(resolve, reject){\n    setTimeout(function(){\n      resolve('promise 2');\n    }, 100);\n  });\n\n  Promise.race([promise1, promise2]).then(function(result){\n    // result === 'promise 2' because it was resolved before promise1\n    // was resolved.\n  });\n  ```\n\n  `Promise.race` is deterministic in that only the state of the first\n  settled promise matters. For example, even if other promises given to the\n  `promises` array argument are resolved, but the first settled promise has\n  become rejected before the other promises became fulfilled, the returned\n  promise will become rejected:\n\n  ```javascript\n  let promise1 = new Promise(function(resolve, reject){\n    setTimeout(function(){\n      resolve('promise 1');\n    }, 200);\n  });\n\n  let promise2 = new Promise(function(resolve, reject){\n    setTimeout(function(){\n      reject(new Error('promise 2'));\n    }, 100);\n  });\n\n  Promise.race([promise1, promise2]).then(function(result){\n    // Code here never runs\n  }, function(reason){\n    // reason.message === 'promise 2' because promise 2 became rejected before\n    // promise 1 became fulfilled\n  });\n  ```\n\n  An example real-world use case is implementing timeouts:\n\n  ```javascript\n  Promise.race([ajax('foo.json'), timeout(5000)])\n  ```\n\n  @method race\n  @static\n  @param {Array} promises array of promises to observe\n  Useful for tooling.\n  @return {Promise} a promise which settles in the same way as the first passed\n  promise to settle.\n*/\nfunction race(entries) {\n  /*jshint validthis:true */\n  var Constructor = this;\n\n  if (!isArray(entries)) {\n    return new Constructor(function (_, reject) {\n      return reject(new TypeError('You must pass an array to race.'));\n    });\n  } else {\n    return new Constructor(function (resolve, reject) {\n      var length = entries.length;\n      for (var i = 0; i < length; i++) {\n        Constructor.resolve(entries[i]).then(resolve, reject);\n      }\n    });\n  }\n}\n\n/**\n  `Promise.reject` returns a promise rejected with the passed `reason`.\n  It is shorthand for the following:\n\n  ```javascript\n  let promise = new Promise(function(resolve, reject){\n    reject(new Error('WHOOPS'));\n  });\n\n  promise.then(function(value){\n    // Code here doesn't run because the promise is rejected!\n  }, function(reason){\n    // reason.message === 'WHOOPS'\n  });\n  ```\n\n  Instead of writing the above, your code now simply becomes the following:\n\n  ```javascript\n  let promise = Promise.reject(new Error('WHOOPS'));\n\n  promise.then(function(value){\n    // Code here doesn't run because the promise is rejected!\n  }, function(reason){\n    // reason.message === 'WHOOPS'\n  });\n  ```\n\n  @method reject\n  @static\n  @param {Any} reason value that the returned promise will be rejected with.\n  Useful for tooling.\n  @return {Promise} a promise rejected with the given `reason`.\n*/\nfunction reject(reason) {\n  /*jshint validthis:true */\n  var Constructor = this;\n  var promise = new Constructor(noop);\n  _reject(promise, reason);\n  return promise;\n}\n\nfunction needsResolver() {\n  throw new TypeError('You must pass a resolver function as the first argument to the promise constructor');\n}\n\nfunction needsNew() {\n  throw new TypeError(\"Failed to construct 'Promise': Please use the 'new' operator, this object constructor cannot be called as a function.\");\n}\n\n/**\n  Promise objects represent the eventual result of an asynchronous operation. The\n  primary way of interacting with a promise is through its `then` method, which\n  registers callbacks to receive either a promise's eventual value or the reason\n  why the promise cannot be fulfilled.\n\n  Terminology\n  -----------\n\n  - `promise` is an object or function with a `then` method whose behavior conforms to this specification.\n  - `thenable` is an object or function that defines a `then` method.\n  - `value` is any legal JavaScript value (including undefined, a thenable, or a promise).\n  - `exception` is a value that is thrown using the throw statement.\n  - `reason` is a value that indicates why a promise was rejected.\n  - `settled` the final resting state of a promise, fulfilled or rejected.\n\n  A promise can be in one of three states: pending, fulfilled, or rejected.\n\n  Promises that are fulfilled have a fulfillment value and are in the fulfilled\n  state.  Promises that are rejected have a rejection reason and are in the\n  rejected state.  A fulfillment value is never a thenable.\n\n  Promises can also be said to *resolve* a value.  If this value is also a\n  promise, then the original promise's settled state will match the value's\n  settled state.  So a promise that *resolves* a promise that rejects will\n  itself reject, and a promise that *resolves* a promise that fulfills will\n  itself fulfill.\n\n\n  Basic Usage:\n  ------------\n\n  ```js\n  let promise = new Promise(function(resolve, reject) {\n    // on success\n    resolve(value);\n\n    // on failure\n    reject(reason);\n  });\n\n  promise.then(function(value) {\n    // on fulfillment\n  }, function(reason) {\n    // on rejection\n  });\n  ```\n\n  Advanced Usage:\n  ---------------\n\n  Promises shine when abstracting away asynchronous interactions such as\n  `XMLHttpRequest`s.\n\n  ```js\n  function getJSON(url) {\n    return new Promise(function(resolve, reject){\n      let xhr = new XMLHttpRequest();\n\n      xhr.open('GET', url);\n      xhr.onreadystatechange = handler;\n      xhr.responseType = 'json';\n      xhr.setRequestHeader('Accept', 'application/json');\n      xhr.send();\n\n      function handler() {\n        if (this.readyState === this.DONE) {\n          if (this.status === 200) {\n            resolve(this.response);\n          } else {\n            reject(new Error('getJSON: `' + url + '` failed with status: [' + this.status + ']'));\n          }\n        }\n      };\n    });\n  }\n\n  getJSON('/posts.json').then(function(json) {\n    // on fulfillment\n  }, function(reason) {\n    // on rejection\n  });\n  ```\n\n  Unlike callbacks, promises are great composable primitives.\n\n  ```js\n  Promise.all([\n    getJSON('/posts'),\n    getJSON('/comments')\n  ]).then(function(values){\n    values[0] // => postsJSON\n    values[1] // => commentsJSON\n\n    return values;\n  });\n  ```\n\n  @class Promise\n  @param {function} resolver\n  Useful for tooling.\n  @constructor\n*/\nfunction Promise(resolver) {\n  this[PROMISE_ID] = nextId();\n  this._result = this._state = undefined;\n  this._subscribers = [];\n\n  if (noop !== resolver) {\n    typeof resolver !== 'function' && needsResolver();\n    this instanceof Promise ? initializePromise(this, resolver) : needsNew();\n  }\n}\n\nPromise.all = all;\nPromise.race = race;\nPromise.resolve = resolve;\nPromise.reject = reject;\nPromise._setScheduler = setScheduler;\nPromise._setAsap = setAsap;\nPromise._asap = asap;\n\nPromise.prototype = {\n  constructor: Promise,\n\n  /**\n    The primary way of interacting with a promise is through its `then` method,\n    which registers callbacks to receive either a promise's eventual value or the\n    reason why the promise cannot be fulfilled.\n  \n    ```js\n    findUser().then(function(user){\n      // user is available\n    }, function(reason){\n      // user is unavailable, and you are given the reason why\n    });\n    ```\n  \n    Chaining\n    --------\n  \n    The return value of `then` is itself a promise.  This second, 'downstream'\n    promise is resolved with the return value of the first promise's fulfillment\n    or rejection handler, or rejected if the handler throws an exception.\n  \n    ```js\n    findUser().then(function (user) {\n      return user.name;\n    }, function (reason) {\n      return 'default name';\n    }).then(function (userName) {\n      // If `findUser` fulfilled, `userName` will be the user's name, otherwise it\n      // will be `'default name'`\n    });\n  \n    findUser().then(function (user) {\n      throw new Error('Found user, but still unhappy');\n    }, function (reason) {\n      throw new Error('`findUser` rejected and we're unhappy');\n    }).then(function (value) {\n      // never reached\n    }, function (reason) {\n      // if `findUser` fulfilled, `reason` will be 'Found user, but still unhappy'.\n      // If `findUser` rejected, `reason` will be '`findUser` rejected and we're unhappy'.\n    });\n    ```\n    If the downstream promise does not specify a rejection handler, rejection reasons will be propagated further downstream.\n  \n    ```js\n    findUser().then(function (user) {\n      throw new PedagogicalException('Upstream error');\n    }).then(function (value) {\n      // never reached\n    }).then(function (value) {\n      // never reached\n    }, function (reason) {\n      // The `PedgagocialException` is propagated all the way down to here\n    });\n    ```\n  \n    Assimilation\n    ------------\n  \n    Sometimes the value you want to propagate to a downstream promise can only be\n    retrieved asynchronously. This can be achieved by returning a promise in the\n    fulfillment or rejection handler. The downstream promise will then be pending\n    until the returned promise is settled. This is called *assimilation*.\n  \n    ```js\n    findUser().then(function (user) {\n      return findCommentsByAuthor(user);\n    }).then(function (comments) {\n      // The user's comments are now available\n    });\n    ```\n  \n    If the assimliated promise rejects, then the downstream promise will also reject.\n  \n    ```js\n    findUser().then(function (user) {\n      return findCommentsByAuthor(user);\n    }).then(function (comments) {\n      // If `findCommentsByAuthor` fulfills, we'll have the value here\n    }, function (reason) {\n      // If `findCommentsByAuthor` rejects, we'll have the reason here\n    });\n    ```\n  \n    Simple Example\n    --------------\n  \n    Synchronous Example\n  \n    ```javascript\n    let result;\n  \n    try {\n      result = findResult();\n      // success\n    } catch(reason) {\n      // failure\n    }\n    ```\n  \n    Errback Example\n  \n    ```js\n    findResult(function(result, err){\n      if (err) {\n        // failure\n      } else {\n        // success\n      }\n    });\n    ```\n  \n    Promise Example;\n  \n    ```javascript\n    findResult().then(function(result){\n      // success\n    }, function(reason){\n      // failure\n    });\n    ```\n  \n    Advanced Example\n    --------------\n  \n    Synchronous Example\n  \n    ```javascript\n    let author, books;\n  \n    try {\n      author = findAuthor();\n      books  = findBooksByAuthor(author);\n      // success\n    } catch(reason) {\n      // failure\n    }\n    ```\n  \n    Errback Example\n  \n    ```js\n  \n    function foundBooks(books) {\n  \n    }\n  \n    function failure(reason) {\n  \n    }\n  \n    findAuthor(function(author, err){\n      if (err) {\n        failure(err);\n        // failure\n      } else {\n        try {\n          findBoooksByAuthor(author, function(books, err) {\n            if (err) {\n              failure(err);\n            } else {\n              try {\n                foundBooks(books);\n              } catch(reason) {\n                failure(reason);\n              }\n            }\n          });\n        } catch(error) {\n          failure(err);\n        }\n        // success\n      }\n    });\n    ```\n  \n    Promise Example;\n  \n    ```javascript\n    findAuthor().\n      then(findBooksByAuthor).\n      then(function(books){\n        // found books\n    }).catch(function(reason){\n      // something went wrong\n    });\n    ```\n  \n    @method then\n    @param {Function} onFulfilled\n    @param {Function} onRejected\n    Useful for tooling.\n    @return {Promise}\n  */\n  then: then,\n\n  /**\n    `catch` is simply sugar for `then(undefined, onRejection)` which makes it the same\n    as the catch block of a try/catch statement.\n  \n    ```js\n    function findAuthor(){\n      throw new Error('couldn't find that author');\n    }\n  \n    // synchronous\n    try {\n      findAuthor();\n    } catch(reason) {\n      // something went wrong\n    }\n  \n    // async with promises\n    findAuthor().catch(function(reason){\n      // something went wrong\n    });\n    ```\n  \n    @method catch\n    @param {Function} onRejection\n    Useful for tooling.\n    @return {Promise}\n  */\n  'catch': function _catch(onRejection) {\n    return this.then(null, onRejection);\n  }\n};\n\nfunction polyfill() {\n    var local = undefined;\n\n    if (typeof global !== 'undefined') {\n        local = global;\n    } else if (typeof self !== 'undefined') {\n        local = self;\n    } else {\n        try {\n            local = Function('return this')();\n        } catch (e) {\n            throw new Error('polyfill failed because global object is unavailable in this environment');\n        }\n    }\n\n    var P = local.Promise;\n\n    if (P) {\n        var promiseToString = null;\n        try {\n            promiseToString = Object.prototype.toString.call(P.resolve());\n        } catch (e) {\n            // silently ignored\n        }\n\n        if (promiseToString === '[object Promise]' && !P.cast) {\n            return;\n        }\n    }\n\n    local.Promise = Promise;\n}\n\n// Strange compat..\nPromise.polyfill = polyfill;\nPromise.Promise = Promise;\n\nreturn Promise;\n\n})));\n//# sourceMappingURL=es6-promise.map\n\n/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(574), __webpack_require__(28)))\n\n//////////////////\n// WEBPACK FOOTER\n// ./~/es6-promise/dist/es6-promise.js\n// module id = 310\n// module chunks = 5\n\n//# sourceURL=webpack:///./~/es6-promise/dist/es6-promise.js?")},319:function(module,exports,__webpack_require__){"use strict";eval("/* WEBPACK VAR INJECTION */(function($) {\n\nObject.defineProperty(exports, \"__esModule\", {\n\tvalue: true\n});\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nvar _gplaces = __webpack_require__(278);\n\nvar _gplaces2 = _interopRequireDefault(_gplaces);\n\nvar _GmapDragSearch = __webpack_require__(333);\n\nvar _GmapDragSearch2 = _interopRequireDefault(_GmapDragSearch);\n\nvar _dragMapList = __webpack_require__(334);\n\nvar _dragMapList2 = _interopRequireDefault(_dragMapList);\n\nvar _tracking = __webpack_require__(11);\n\nvar _tracking2 = _interopRequireDefault(_tracking);\n\nvar _loader = __webpack_require__(72);\n\nvar _loader2 = _interopRequireDefault(_loader);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n__webpack_require__(63);\n__webpack_require__(19);\n__webpack_require__(552);\n\nvar Beerlocator = function () {\n\tfunction Beerlocator(selector) {\n\t\tvar _this2 = this;\n\n\t\t_classCallCheck(this, Beerlocator);\n\n\t\tvar _this = this;\n\t\tthis.data = {};\n\t\tthis.category = 'stores';\n\t\tthis.currentFilter = '';\n\t\tthis.filter = 'all';\n\t\tthis.maplist = new _dragMapList2.default('#results');\n\t\tthis.map = new _GmapDragSearch2.default({ lat: 40.73186, lng: -73.98863 });\n\t\tthis.input = document.getElementById(\"ZipCode\");\n\t\tthis.input.value = '10003';\n\t\t_gplaces2.default.autoComplete(this.input);\n\t\t_gplaces2.default.getFirstSuggestion(this.input);\n\n\t\t$(document).ready(function () {\n\t\t\t_this2.addUIListeners();\n\t\t\t$('.mapview,.fb-list').addClass('hide');\n\t\t});\n\n\t\t$('#ZipCode').on('blur', function (e) {\n\t\t\tvar v = this.value;\n\t\t\tif (this.value.length == 0) this.value = v;\n\t\t\t_this.scrollWindow();\n\t\t}).on('focus', function () {\n\t\t\tthis.value = \"\";\n\t\t});\n\n\t\twindow.dispatcher.on('onShowMap', function () {\n\t\t\treturn _this2.showMap();\n\t\t});\n\t\twindow.dispatcher.on('onFirstSuggestion', function () {\n\t\t\treturn _this2.scrollWindow();\n\t\t});\n\t\twindow.dispatcher.on('onSetFilterSelected', function (e) {\n\t\t\treturn _this2.setFilterSelected(e[0]);\n\t\t});\n\t\twindow.dispatcher.on('onNoLocationsFound', function (e) {\n\t\t\treturn _this2.showMap();\n\t\t});\n\t}\n\n\t_createClass(Beerlocator, [{\n\t\tkey: 'filterProducts',\n\t\tvalue: function filterProducts(id) {\n\t\t\tthis.filter = id;\n\t\t\twindow.dispatcher.trigger('onFilterChange');\n\t\t\tthis.setFilterSelected(this.filter);\n\t\t\tthis.scrollWindow();\n\t\t}\n\t}, {\n\t\tkey: 'setFilterSelected',\n\t\tvalue: function setFilterSelected(id) {\n\t\t\tif (id !== undefined) {\n\t\t\t\t$('input.filter[data-filter=' + id + ']').attr('checked', true);\n\t\t\t\t$('input.filter[data-filter!=' + id + ']').attr('checked', false);\n\t\t\t}\n\t\t}\n\t}, {\n\t\tkey: 'scrollWindow',\n\t\tvalue: function scrollWindow() {\n\t\t\tif (window.screen.width < 769) $('html, body').animate({ scrollTop: 600 }, 800);\n\t\t}\n\t}, {\n\t\tkey: 'addUIListeners',\n\t\tvalue: function addUIListeners() {\n\t\t\tvar _this3 = this;\n\n\t\t\tvar _this = this;\n\t\t\t$('.viewlist').on(\"click\", function (e) {\n\t\t\t\treturn _this3.hideMap();\n\t\t\t});\n\t\t\t$('.mapview').on(\"click\", function (e) {\n\t\t\t\treturn _this3.showMap();\n\t\t\t});\n\t\t\t$(\".bars, .stores\").on(\"click\", function (e) {\n\t\t\t\t_this3.scrollWindow();window.dispatcher.trigger('onTypeChange');\n\t\t\t});\n\t\t\t$('.filter').on('click', function (e) {\n\t\t\t\treturn _this3.filterProducts($(e.target).data('filter'));\n\t\t\t});\n\t\t\t//===================================LOCATE ME\n\t\t\t$('.locateme').on('click', function (e) {\n\t\t\t\te.preventDefault();\n\t\t\t\twindow.dispatcher.trigger('onShowLoader');\n\t\t\t\tif (navigator.geolocation) {\n\t\t\t\t\tvar geo_success = function geo_success(position) {\n\t\t\t\t\t\tvar pos = {\n\t\t\t\t\t\t\tlat: position.coords.latitude,\n\t\t\t\t\t\t\tlng: position.coords.longitude\n\t\t\t\t\t\t};\n\t\t\t\t\t\t_this.map.address = 'street_address';\n\t\t\t\t\t\twindow.dispatcher.trigger('onInitMap', [pos]);\n\t\t\t\t\t\t_this.scrollWindow();\n\t\t\t\t\t\t_this.maplist.reset();\n\t\t\t\t\t\t_gplaces2.default.getAddress(pos).then(function (zipcode) {\n\t\t\t\t\t\t\t_this.input.value = zipcode;\n\t\t\t\t\t\t});\n\t\t\t\t\t};\n\n\t\t\t\t\tvar geo_error = function geo_error(err) {\n\t\t\t\t\t\twindow.dispatcher.trigger('onHideLoader');\n\t\t\t\t\t\t_this.scrollWindow();\n\t\t\t\t\t\t_this.map.showErrorMsg('Geolocation is not available');\n\t\t\t\t\t};\n\n\t\t\t\t\tnavigator.geolocation.getCurrentPosition(geo_success, geo_error);\n\t\t\t\t}\n\t\t\t});\n\t\t}\n\t}, {\n\t\tkey: 'hideMap',\n\t\tvalue: function hideMap() {\n\t\t\tif (!this.maplist.listItems.length) return;\n\t\t\t$('.fb-list').removeClass('hide');\n\t\t\t$('.mapview').removeClass('hide');\n\t\t\t$('.viewlist').addClass('hide');\n\t\t\t$('.fb-map').css('visibility', 'hidden');\n\t\t\tthis.map.info_window.close();\n\t\t\tthis.maplist.setListView(true);\n\t\t}\n\t}, {\n\t\tkey: 'showMap',\n\t\tvalue: function showMap() {\n\t\t\t$('.viewlist,.mapview').removeClass('hide');\n\t\t\t$('.mapview,.fb-list').addClass('hide');\n\t\t\t$('.fb-map').css('visibility', 'visible');\n\t\t\tthis.maplist.setListView(false);\n\t\t}\n\t}]);\n\n\treturn Beerlocator;\n}();\n\nexports.default = Beerlocator;\n\nvar barfinder = new Beerlocator('.beerlocator');\n/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(1)))\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/beerlocator/beerlocator.js\n// module id = 319\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/beerlocator/beerlocator.js?")},333:function(module,exports,__webpack_require__){"use strict";eval("/* WEBPACK VAR INJECTION */(function($, _) {\n\nObject.defineProperty(exports, \"__esModule\", {\n\tvalue: true\n});\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nvar _loader = __webpack_require__(72);\n\nvar _loader2 = _interopRequireDefault(_loader);\n\nvar _tracking = __webpack_require__(11);\n\nvar _tracking2 = _interopRequireDefault(_tracking);\n\nvar _googleMaps = __webpack_require__(118);\n\nvar _googleMaps2 = _interopRequireDefault(_googleMaps);\n\nvar _gplaces = __webpack_require__(278);\n\nvar _gplaces2 = _interopRequireDefault(_gplaces);\n\nvar _pulseMarker = __webpack_require__(339);\n\nvar _pulseMarker2 = _interopRequireDefault(_pulseMarker);\n\nvar _gmapTilemarker = __webpack_require__(357);\n\nvar _gmapTilemarker2 = _interopRequireDefault(_gmapTilemarker);\n\nvar _gmapMarkercluster = __webpack_require__(356);\n\nvar _gmapMarkercluster2 = _interopRequireDefault(_gmapMarkercluster);\n\nvar _geturlparams = __webpack_require__(355);\n\nvar _google_api = __webpack_require__(158);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\nvar template = __webpack_require__(569);\n\nvar roadAtlasStyles = __webpack_require__(335).style;\n\nvar Map = function () {\n\tfunction Map(loc) {\n\t\tvar _this2 = this;\n\n\t\t_classCallCheck(this, Map);\n\n\t\tif (!loc) {}\n\t\tvar _this = this;\n\t\tthis.defaultLoc = { lat: loc.lat, lng: loc.lng };\n\t\tthis.mymarkers = [];\n\t\tthis.pulseMarker = _pulseMarker2.default;\n\t\tthis.numLocationsToLoad = 0;\n\t\tthis.mapCenter;\n\t\tthis.markerCluster;\n\t\tthis.hasBeenResizedToFitBounds = false;\n\t\tthis.loader = new _loader2.default($('.fb-map'));\n\t\tthis.searchData = {};\n\t\tthis.zipcode;\n\t\tthis.address = 'postal_code';\n\t\tthis.xhrPool = [];\n\t\t_googleMaps2.default.KEY = (0, _google_api.googleApiKey)();\n\t\t_googleMaps2.default.LIBRARIES = ['places'];\n\t\t_googleMaps2.default.load(function (google) {\n\t\t\twindow.dispatcher.on('onShowLoader', function (e) {\n\t\t\t\treturn _this2.showLoader();\n\t\t\t});\n\t\t\twindow.dispatcher.on('onHideLoader', function (e) {\n\t\t\t\treturn _this2.hideLoader();\n\t\t\t});\n\t\t\twindow.dispatcher.on('onInitMap', function (e) {\n\t\t\t\treturn _this2.initMap(e[0]);\n\t\t\t});\n\t\t\twindow.dispatcher.on('onGPlacesError', function (e) {\n\t\t\t\treturn _this2.initMap(_this2.defaultLoc);\n\t\t\t});\n\t\t\twindow.dispatcher.on(\"onGPlacesChanged\", function (location, address) {\n\t\t\t\tvar latlng = {\n\t\t\t\t\tlat: location[0].lat(),\n\t\t\t\t\tlng: location[0].lng()\n\t\t\t\t};\n\t\t\t\t_this2.address = location[1];\n\n\t\t\t\t_this2.initMap(latlng);\n\t\t\t});\n\t\t\twindow.dispatcher.on('onFilterChange', function (e) {\n\t\t\t\t_this2.abortAjaxCalls();\n\t\t\t\t_this2.addLocations();\n\t\t\t});\n\t\t\twindow.dispatcher.on('onTypeChange', function (e) {\n\t\t\t\t_this2.abortAjaxCalls();\n\t\t\t\t_this2.addLocations();\n\t\t\t});\n\t\t\tif ((0, _geturlparams.GetQueryStringParams)('zipcode') || (0, _geturlparams.GetQueryStringParams)('product')) {\n\t\t\t\tvar product = (0, _geturlparams.GetQueryStringParams)('product');\n\t\t\t\tvar newzip = (0, _geturlparams.GetQueryStringParams)('zipcode');\n\t\t\t\twindow.dispatcher.trigger('onSetFilterSelected', [product]);\n\t\t\t\t_this2.setQueryParams(newzip);\n\t\t\t} else {\n\t\t\t\t_this2.initMap(_this2.defaultLoc);\n\t\t\t}\n\t\t});\n\t}\n\n\t_createClass(Map, [{\n\t\tkey: 'setQueryParams',\n\t\tvalue: function setQueryParams(newzip) {\n\t\t\tvar _this3 = this;\n\n\t\t\tthis.zipcode = newzip;\n\t\t\tdocument.getElementById('ZipCode').value = newzip;\n\t\t\tthis.searchData.ZipCode = newzip;\n\t\t\t_gplaces2.default.getLatLng(newzip).then(function (latlng) {\n\t\t\t\t_this3.initMap(latlng);\n\t\t\t});\n\t\t}\n\t}, {\n\t\tkey: 'initMap',\n\t\tvalue: function initMap(loc) {\n\t\t\tvar _this4 = this;\n\n\t\t\tthis.showLoader();\n\t\t\tthis.hasBeenResizedToFitBounds = false;\n\t\t\tthis.bounds = new google.maps.LatLngBounds();\n\t\t\tthis.map = new google.maps.Map(document.getElementById('my_map'), {\n\t\t\t\tclickableIcons: false,\n\t\t\t\tname: 'Dos Equis',\n\t\t\t\tstreetViewControl: false,\n\t\t\t\tmapTypeControl: false,\n\t\t\t\tmaxZoom: 17,\n\t\t\t\tminZoom: 10,\n\t\t\t\tfullscreenControl: 0,\n\t\t\t\tgestureHandling: 'greedy'\n\t\t\t});\n\t\t\t//SET different Zoom Levels\n\t\t\tvar zoom = $(window).width() < 767 ? this.map.setZoom(14) : this.map.setZoom(15);\n\t\t\tthis.map.setCenter(loc);\n\t\t\tthis.map.addListener('zoom_changed', function () {\n\t\t\t\t//console.log('zoom = ', this.map.getZoom())\n\t\t\t});\n\t\t\t//==============================================================\n\t\t\twindow.dispatcher.on(\"onSetMapItem\", function (e) {\n\t\t\t\treturn _this4.setMapItem(e[0]);\n\t\t\t});\n\t\t\tvar usRoadMapType = new google.maps.StyledMapType(roadAtlasStyles);\n\t\t\tthis.info_window = new google.maps.InfoWindow({\n\t\t\t\t//pixelOffset: new google.maps.Size(0, 60)\n\t\t\t\tmaxWidth: 300\n\t\t\t});\n\t\t\tthis.map.bounds = new google.maps.LatLngBounds();\n\t\t\tthis.map.mapTypes.set('usroadatlas', usRoadMapType);\n\t\t\tthis.map.setMapTypeId('usroadatlas');\n\t\t\tthis.listItems = [];\n\t\t\tthis.markerCluster = new _gmapMarkercluster2.default.MarkerClusterer(this.map, null, {\n\t\t\t\timagePath: '/Assets/Images/gmap-cluster/de-m',\n\t\t\t\tmaxZoom: 14\n\t\t\t});\n\t\t\tthis.listItemIndex = 0;\n\n\t\t\tif (this.centerMarker) this.centerMarker.setMap(null);\n\t\t\tvar image = this.address == 'street_address' ? this.addPulseMarker() : this.addZipCodeMarker();\n\t\t\tthis.addLocations(null);\n\t\t}\n\t}, {\n\t\tkey: 'abortAjaxCalls',\n\t\tvalue: function abortAjaxCalls() {\n\t\t\t$.each(this.xhrPool, function (idx, jqXHR) {\n\t\t\t\tjqXHR.abort();\n\t\t\t});\n\t\t\tthis.xhrPool = [];\n\t\t}\n\t}, {\n\t\tkey: 'addLocations',\n\t\tvalue: function addLocations(center) {\n\t\t\tthis.showLoader();\n\t\t\tthis.numLocationsToLoad = 0;\n\t\t\tthis.xhrPool = [];\n\t\t\tif (this.errorMarker) this.errorMarker.setMap(null);\n\t\t\tthis.deleteMarkers();\n\t\t\tthis.tileMarkerMapType();\n\t\t}\n\t}, {\n\t\tkey: 'tileMarkerMapType',\n\t\tvalue: function tileMarkerMapType() {\n\t\t\tvar _this = this;\n\t\t\tthis.markerMap = {};\n\t\t\tthis.mapCenter = this.map.getCenter();\n\t\t\tthis.map.overlayMapTypes.insertAt(0, new _gmapTilemarker2.default.TileMarkerMapType(this.map, function (center, radiusMiles, NW, SE) {\n\t\t\t\t_this.radius = radiusMiles;\n\t\t\t\t_this.center = center;\n\t\t\t\t_this.searchData = {\n\t\t\t\t\tlatitude: center.lat,\n\t\t\t\t\tlongitude: center.lng,\n\t\t\t\t\tsearchRadiusInMiles: radiusMiles\n\t\t\t\t};\n\t\t\t\t_this.searchAPI().done(function (data) {});\n\t\t\t}));\n\t\t}\n\t}, {\n\t\tkey: 'searchAPI',\n\t\tvalue: function searchAPI() {\n\t\t\tvar _this5 = this;\n\n\t\t\tvar _this = this;\n\t\t\tthis.searchData.brands = $(\"input:radio[name='product']:checked\").val();\n\t\t\tthis.searchData.storeCodes = $(\"input:radio[name='type']:checked\").val();\n\t\t\tthis.searchData.ZipCode = this.zipcode;\n\t\t\tthis.searchData.fetchall = false;\n\t\t\tthis.searchData.centerLatitude = this.mapCenter.lat();\n\t\t\tthis.searchData.centerLongitude = this.mapCenter.lng();\n\t\t\treturn $.ajax({\n\t\t\t\tdataType: \"json\",\n\t\t\t\turl: '/api/BarFinder/Search',\n\t\t\t\tdata: this.searchData,\n\t\t\t\tbeforeSend: function beforeSend(xhr) {\n\t\t\t\t\t_this.xhrPool.push(xhr);\n\t\t\t\t},\n\t\t\t\tsuccess: function success(data) {\n\t\t\t\t\t_this.xhrPool.shift();\n\t\t\t\t\tif (!_this.xhrPool.length) {\n\t\t\t\t\t\t_this5.ajaxStop();\n\t\t\t\t\t\treturn;\n\t\t\t\t\t} else {\n\t\t\t\t\t\t$.each(data.SearchResults, function (i, location) {\n\t\t\t\t\t\t\t_this5.addMarkerCluster(i, location);\n\t\t\t\t\t\t});\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t});\n\t\t}\n\t}, {\n\t\tkey: 'ajaxStop',\n\t\tvalue: function ajaxStop() {\n\t\t\twindow.dispatcher.trigger('onLocationsDoneLoading');\n\t\t\t//if(this.mymarkers.length){\n\t\t\tif (this.mymarkers.length <= 20 && !this.hasBeenResizedToFitBounds) {\n\t\t\t\tthis.fitBoundsToMarkers();\n\t\t\t}\n\t\t\t//}\n\t\t\telse if (!this.mymarkers.length && this.hasBeenResizedToFitBounds) {\n\t\t\t\t\tif (this.errorMarker) this.errorMarker.setMap(null);\n\t\t\t\t\tthis.showErrorMsg('No Locations were found.</br> Keep staying thirsty.');\n\t\t\t\t\twindow.dispatcher.trigger('onNoLocationsFound');\n\t\t\t\t}\n\t\t\tthis.hideLoader();\n\t\t}\n\t}, {\n\t\tkey: 'addMarkerCluster',\n\t\tvalue: function addMarkerCluster(i, location) {\n\t\t\tvar _this6 = this;\n\n\t\t\tvar _this = this;\n\t\t\tif (this.errorMarker) this.errorMarker.setMap(null);\n\t\t\tlocation.id = _this.listItemIndex;\n\t\t\tvar mKey = location.Latitude + ',' + location.Longitude;\n\t\t\tif (this.markerMap[mKey] !== true) {\n\t\t\t\tthis.markerMap[mKey] = true;\n\t\t\t\tvar marker = new google.maps.Marker({\n\t\t\t\t\tposition: {\n\t\t\t\t\t\tlat: location.Latitude,\n\t\t\t\t\t\tlng: location.Longitude\n\t\t\t\t\t},\n\t\t\t\t\ticon: '/Assets/Images/beerlocator/pin.png',\n\t\t\t\t\tid: this.listItemIndex,\n\t\t\t\t\tName: location.Name,\n\t\t\t\t\ttitle: location.Name + ' - ' + location.Address + '- ' + location.Distance\n\t\t\t\t});\n\t\t\t\tthis.bounds.extend(marker.getPosition());\n\t\t\t\tthis.listItemIndex++;\n\t\t\t\tmarker.addListener('click', function (e) {\n\t\t\t\t\t_this6.info_window.setContent(template(location));\n\t\t\t\t\t_this6.info_window.open(_this6.map, marker);\n\t\t\t\t\t_tracking2.default.trackEvent({\n\t\t\t\t\t\tlabel: location.Name,\n\t\t\t\t\t\tcategory: 'map-pin'\n\t\t\t\t\t});\n\t\t\t\t});\n\t\t\t\tmarker.setZIndex(1);\n\t\t\t\tthis.mymarkers.push(marker);\n\t\t\t\tthis.markerCluster.addMarker(marker);\n\t\t\t\tif (this.mymarkers.length) window.dispatcher.trigger('addListItem', [location]);\n\t\t\t}\n\t\t}\n\t}, {\n\t\tkey: 'setMapItem',\n\t\tvalue: function setMapItem(id) {\n\t\t\tvar marker = _.findWhere(this.mymarkers, { id: id });\n\t\t\tgoogle.maps.event.trigger(marker, \"click\");\n\t\t\tthis.map.setZoom(15);\n\t\t\tthis.map.panTo(marker.position);\n\t\t\tsetTimeout(function () {\n\t\t\t\twindow.dispatcher.trigger('onShowMap');\n\t\t\t}, 400);\n\t\t}\n\t}, {\n\t\tkey: 'deleteMarkers',\n\t\tvalue: function deleteMarkers() {\n\t\t\tfor (var i = 0; i < this.mymarkers.length; i++) {\n\t\t\t\tthis.mymarkers[i].setMap(null);\n\t\t\t}\n\t\t\tif (this.mymarkers.length) this.markerCluster.clearMarkers();\n\t\t\tthis.mymarkers = [];\n\t\t\tthis.mymarkers.length = 0;\n\t\t}\n\t}, {\n\t\tkey: 'showErrorMsg',\n\t\tvalue: function showErrorMsg(msg) {\n\t\t\tvar _this = this;\n\t\t\tthis.errorMarker = new google.maps.Marker({\n\t\t\t\tposition: this.map.getCenter(),\n\t\t\t\tmap: this.map\n\t\t\t});\n\t\t\tthis.info_window.setContent(\"<p class='error'>\" + msg + \"</p>\");\n\t\t\tsetTimeout(function () {\n\t\t\t\t_this.info_window.open(_this.map, _this.errorMarker);\n\t\t\t}, 350);\n\t\t\tgoogle.maps.event.addListener(_this.info_window, 'closeclick', function () {\n\t\t\t\tif (_this.errorMarker) {\n\t\t\t\t\t_this.errorMarker.setMap(null);\n\t\t\t\t\t_this.errorMarker = null;\n\t\t\t\t}\n\t\t\t});\n\t\t}\n\t}, {\n\t\tkey: 'fitBoundsToMarkers',\n\t\tvalue: function fitBoundsToMarkers() {\n\t\t\tvar zoom = $(window).width() < 769 ? this.map.setZoom(12) : this.map.setZoom(13);\n\t\t\t//if(this.hasBeenResizedToFitBounds)return;\n\n\t\t\tthis.hasBeenResizedToFitBounds = true;\n\t\t\tif (!this.mymarkers.length) {\n\t\t\t\twindow.dispatcher.trigger('onNoLocationsFound');\n\t\t\t}\n\t\t}\n\t}, {\n\t\tkey: 'showLoader',\n\t\tvalue: function showLoader() {\n\t\t\tthis.loader.show();\n\t\t}\n\t}, {\n\t\tkey: 'hideLoader',\n\t\tvalue: function hideLoader() {\n\t\t\tthis.loader.hide();\n\t\t}\n\t}, {\n\t\tkey: 'addZipCodeMarker',\n\t\tvalue: function addZipCodeMarker() {\n\t\t\tthis.centerMarker = new google.maps.Marker({\n\t\t\t\tposition: this.map.getCenter(),\n\t\t\t\tmap: this.map,\n\t\t\t\ttitle: 'XX marks the spot',\n\t\t\t\ticon: '/assets/images/beerlocator/nearby_icon.png'\n\t\t\t});\n\t\t}\n\t}, {\n\t\tkey: 'addPulseMarker',\n\t\tvalue: function addPulseMarker() {\n\t\t\tthis.pulsemarker = new _pulseMarker2.default({\n\t\t\t\tposition: this.map.getCenter(),\n\t\t\t\tmap: this.map\n\t\t\t});\n\t\t}\n\t}]);\n\n\treturn Map;\n}();\n\nexports.default = Map;\n/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(1), __webpack_require__(95)))\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/beerlocator/GmapDragSearch.js\n// module id = 333\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/beerlocator/GmapDragSearch.js?")},334:function(module,exports,__webpack_require__){"use strict";eval("/* WEBPACK VAR INJECTION */(function($) {\n\nObject.defineProperty(exports, \"__esModule\", {\n\tvalue: true\n});\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nvar _tracking = __webpack_require__(11);\n\nvar _tracking2 = _interopRequireDefault(_tracking);\n\nvar _getPaginatedItems = __webpack_require__(354);\n\nvar _loader = __webpack_require__(72);\n\nvar _loader2 = _interopRequireDefault(_loader);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\nvar template = __webpack_require__(570);\n\nvar MapList = function () {\n\tfunction MapList(selector) {\n\t\tvar _this = this;\n\n\t\t_classCallCheck(this, MapList);\n\n\t\tthis.listItems = [];\n\t\tthis.currentPage = 1;\n\t\tthis.index = 0;\n\t\tthis.isListView = false;\n\t\tthis.itemsPerPage = 20;\n\t\twindow.dispatcher.on('addListItem', function (e) {\n\t\t\treturn _this.add_item(e[0]);\n\t\t});\n\t\twindow.dispatcher.on('onTypeChange', function (e) {\n\t\t\treturn _this.reset();\n\t\t});\n\t\twindow.dispatcher.on('onFilterChange', function (e) {\n\t\t\treturn _this.reset();\n\t\t});\n\t\twindow.dispatcher.on(\"onGPlacesChanged\", function (e) {\n\t\t\treturn _this.reset();\n\t\t});\n\t\twindow.dispatcher.on(\"onLatLngChanged\", function (e) {\n\t\t\treturn _this.reset();\n\t\t});\n\t\twindow.dispatcher.on(\"onLocationsDoneLoading\", function (e) {\n\t\t\treturn _this.renderListItems();\n\t\t});\n\t\twindow.dispatcher.on('onNoLocationsFound', function (e) {\n\t\t\treturn _this.showError();\n\t\t});\n\t\twindow.dispatcher.on('onShowList', function (e) {\n\t\t\treturn _this.setListView(e[0]);\n\t\t});\n\t\tthis.loader = new _loader2.default('.fb-list');\n\t\tthis.$loadmore = $(\".loadmore\");\n\t\tthis.$loadmore.addClass('hide');\n\t\tthis.$loadmore.on('click', function (e) {\n\t\t\t_this.currentPage++;\n\t\t\t_this.loadNextPage(_this.listItems);\n\t\t});\n\t\t$('.error-noresults').addClass('hide');\n\t}\n\n\t_createClass(MapList, [{\n\t\tkey: 'setListView',\n\t\tvalue: function setListView(bool) {\n\t\t\tvar b = bool ? this.isListView = true : this.isListView = false;\n\t\t\tif (this.isListView) var items = this.getItems(this.listItems);\n\t\t\tthis.loadNextPage(this.listItems);\n\t\t}\n\t}, {\n\t\tkey: 'renderListItems',\n\t\tvalue: function renderListItems() {\n\t\t\tif (!this.listItems.length) {\n\t\t\t\tthis.showError();\n\t\t\t} else if (this.listItems.length && this.isListView) {\n\t\t\t\tvar items = this.getItems(this.listItems);\n\t\t\t\tthis.loadNextPage(items);\n\t\t\t}\n\t\t}\n\t}, {\n\t\tkey: 'showError',\n\t\tvalue: function showError() {\n\t\t\tthis.loader.hide();\n\t\t}\n\t}, {\n\t\tkey: 'getItems',\n\t\tvalue: function getItems(items) {\n\t\t\tvar arr = items;\n\t\t\tarr.sort(function (a, b) {\n\t\t\t\tvar x = a.Distance,\n\t\t\t\t    y = b.Distance;\n\t\t\t\treturn x < y ? -1 : x > y ? 1 : 0;\n\t\t\t});\n\t\t\treturn arr;\n\t\t}\n\t}, {\n\t\tkey: 'loadNextPage',\n\t\tvalue: function loadNextPage(items) {\n\t\t\tvar _this2 = this;\n\n\t\t\tthis.loader.hide();\n\t\t\tvar pageItems = (0, _getPaginatedItems.getPaginatedItems)(items, this.currentPage, this.itemsPerPage);\n\t\t\tvar items = pageItems.data;\n\t\t\tif (this.currentPage == 1) $(\".resultsholder\").empty();\n\t\t\titems.forEach(function (model, ind) {\n\t\t\t\tvar $div = $(template(model)).appendTo($('.resultsholder'));\n\t\t\t\t$div.on('click', function (e) {\n\t\t\t\t\treturn _this2.setListItemSelected(model.id);\n\t\t\t\t});\n\t\t\t});\n\t\t\tpageItems.total_pages > this.currentPage ? $('.loadmore').removeClass('hide') : $('.loadmore').addClass('hide');\n\t\t\t//console.log($('.resultsholder').children().length)\n\t\t}\n\t}, {\n\t\tkey: 'add_item',\n\t\tvalue: function add_item(model) {\n\t\t\tthis.listItems.push(model);\n\t\t\tthis.index++;\n\t\t\t$('.count-topnumber').html(this.index);\n\t\t}\n\t}, {\n\t\tkey: 'setListItemSelected',\n\t\tvalue: function setListItemSelected(id) {\n\t\t\t$('.bar[data-id=' + id + ']').addClass('selected');\n\t\t\t$('.bar[data-id!=' + id + ']').removeClass('selected');\n\t\t\twindow.dispatcher.trigger('onSetMapItem', [id]);\n\t\t}\n\t}, {\n\t\tkey: 'reset',\n\t\tvalue: function reset() {\n\t\t\t$('.loadmore,.error-noresults').addClass('hide');\n\t\t\tthis.loader.show();\n\t\t\tthis.listItems = [];\n\t\t\tthis.index = 0;\n\t\t\tthis.currentPage = 1;\n\t\t\t$(\".resultsholder\").empty();\n\t\t\t$('.count-topnumber').html('0');\n\t\t}\n\t}]);\n\n\treturn MapList;\n}();\n\nexports.default = MapList;\n/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(1)))\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/beerlocator/dragMapList.js\n// module id = 334\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/beerlocator/dragMapList.js?")},335:function(module,exports,__webpack_require__){"use strict";eval('\n\nexports.style = [{\n\t"featureType": "water",\n\t"elementType": "geometry",\n\t"stylers": [{\n\t\t"color": "#e9e9e9"\n\t}, {\n\t\t"lightness": 17\n\t}]\n}, {\n\t"featureType": "landscape",\n\t"elementType": "geometry",\n\t"stylers": [{\n\t\t"color": "#f5f5f5"\n\t}, {\n\t\t"lightness": 20\n\t}]\n}, {\n\t"featureType": "road.highway",\n\t"elementType": "geometry.fill",\n\t"stylers": [{\n\t\t"color": "#ffffff"\n\t}, {\n\t\t"lightness": 17\n\t}]\n}, {\n\t"featureType": "road.highway",\n\t"elementType": "geometry.stroke",\n\t"stylers": [{\n\t\t"color": "#ffffff"\n\t}, {\n\t\t"lightness": 29\n\t}, {\n\t\t"weight": 0.2\n\t}]\n}, {\n\t"featureType": "road.arterial",\n\t"elementType": "geometry",\n\t"stylers": [{\n\t\t"color": "#ffffff"\n\t}, {\n\t\t"lightness": 18\n\t}]\n}, {\n\t"featureType": "road.local",\n\t"elementType": "geometry",\n\t"stylers": [{\n\t\t"color": "#ffffff"\n\t}, {\n\t\t"lightness": 16\n\t}]\n}, {\n\t"featureType": "poi",\n\t"elementType": "geometry",\n\t"stylers": [{\n\t\t"color": "#f5f5f5"\n\t}, {\n\t\t"lightness": 21\n\t}]\n}, {\n\t"featureType": "poi.park",\n\t"elementType": "geometry",\n\t"stylers": [{\n\t\t"color": "#dedede"\n\t}, {\n\t\t"lightness": 21\n\t}]\n}, {\n\t"elementType": "labels.text.stroke",\n\t"stylers": [{\n\t\t"visibility": "on"\n\t}, {\n\t\t"color": "#ffffff"\n\t}, {\n\t\t"lightness": 16\n\t}]\n}, {\n\t"elementType": "labels.text.fill",\n\t"stylers": [{\n\t\t"saturation": 36\n\t}, {\n\t\t"color": "#333333"\n\t}, {\n\t\t"lightness": 40\n\t}]\n}, {\n\t"elementType": "labels.icon",\n\t"stylers": [{\n\t\t"visibility": "off"\n\t}]\n}, {\n\t"featureType": "transit",\n\t"elementType": "geometry",\n\t"stylers": [{\n\t\t"color": "#f2f2f2"\n\t}, {\n\t\t"lightness": 19\n\t}]\n}, {\n\t"featureType": "administrative",\n\t"elementType": "geometry.fill",\n\t"stylers": [{\n\t\t"color": "#fefefe"\n\t}, {\n\t\t"lightness": 20\n\t}]\n}, {\n\t"featureType": "administrative",\n\t"elementType": "geometry.stroke",\n\t"stylers": [{\n\t\t"color": "#fefefe"\n\t}, {\n\t\t"lightness": 17\n\t}, {\n\t\t"weight": 1.2\n\t}]\n}];\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/beerlocator/mapstyles.js\n// module id = 335\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/beerlocator/mapstyles.js?')},339:function(module,exports,__webpack_require__){"use strict";eval("/* WEBPACK VAR INJECTION */(function($) {\n\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n\nvar _googleMaps = __webpack_require__(118);\n\nvar _googleMaps2 = _interopRequireDefault(_googleMaps);\n\nvar _google_api = __webpack_require__(158);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n__webpack_require__(547);\n\nvar PulseMarker = function PulseMarker(opts) {\n    var _this2 = this;\n\n    _classCallCheck(this, PulseMarker);\n\n    var _this = this;\n    _googleMaps2.default.KEY = (0, _google_api.googleApiKey)();\n    _googleMaps2.default.load(function (google) {\n        _this2.prototype = new google.maps.OverlayView();\n        _this2.prototype.setValues(opts);\n        _this2.prototype.draw = function () {\n            var self = this;\n            this.d = this.div;\n            if (!this.d) {\n                this.d = this.div = $('' + '<div>' + '<div class=\"shadow\"></div>' + '<div class=\"pulse\"></div>' + '<div class=\"pin-wrap\">' + '<div class=\"pin\"></div>' + '</div>' + '</div>' + '')[0];\n                this.pinWrap = this.div.getElementsByClassName('pin-wrap');\n                this.pin = this.div.getElementsByClassName('pin');\n                this.pinShadow = this.div.getElementsByClassName('shadow');\n                this.d.style.position = 'absolute';\n                this.d.style.cursor = 'pointer';\n                var panes = this.getPanes();\n                panes.overlayImage.appendChild(this.d);\n            }\n            var point = this.getProjection().fromLatLngToDivPixel(this.position);\n            if (point) {\n                this.d.style.left = point.x + 'px';\n                this.d.style.top = point.y + 'px';\n            }\n        };\n    });\n};\n\nexports.default = PulseMarker;\n/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(1)))\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/components/pulseMarker.js\n// module id = 339\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/components/pulseMarker.js?")},354:function(module,exports,__webpack_require__){"use strict";eval('\n\nObject.defineProperty(exports, "__esModule", {\n  value: true\n});\nexports.getPaginatedItems = getPaginatedItems;\nfunction getPaginatedItems(arr, page, itemsPerPage) {\n  var page = page || 1,\n      per_page = itemsPerPage,\n      offset = (page - 1) * per_page,\n      paginatedItems = arr.slice(offset, per_page * page);\n  return {\n    page: page,\n    per_page: per_page,\n    total: arr.length,\n    total_pages: Math.ceil(arr.length / per_page),\n    data: paginatedItems\n  };\n}\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/utils/getPaginatedItems.js\n// module id = 354\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/utils/getPaginatedItems.js?')},355:function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\nexports.GetQueryStringParams = GetQueryStringParams;\nfunction GetQueryStringParams(param1) {\n  if (window.location.search) {\n    var GetQueryStringParams = function GetQueryStringParams(sParam) {\n      var sPageURL = window.location.search.substring(1);\n      var sURLVariables = sPageURL.split('&');\n      for (var i = 0; i < sURLVariables.length; i++) {\n        var sParameterName = sURLVariables[i].split('=');\n        if (sParameterName[0] == sParam) {\n          return sParameterName[1];\n        }\n      }\n    };\n    var item = GetQueryStringParams(param1);\n  }\n  return item;\n}\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/utils/geturlparams.js\n// module id = 355\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/utils/geturlparams.js?")},356:function(module,exports,__webpack_require__){"use strict";eval("\n\nvar _typeof = typeof Symbol === \"function\" && typeof Symbol.iterator === \"symbol\" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === \"function\" && obj.constructor === Symbol && obj !== Symbol.prototype ? \"symbol\" : typeof obj; };\n\n/**\n * @name MarkerClusterer for Google Maps v3\n * @version version 1.0.1\n * @author Luke Mahe\n * @fileoverview\n * The library creates and manages per-zoom-level clusters for large amounts of\n * markers.\n * <br/>\n * This is a v3 implementation of the\n * <a href=\"http://gmaps-utility-library-dev.googlecode.com/svn/tags/markerclusterer/\"\n * >v2 MarkerClusterer</a>.\n */\n\n/**\n * Licensed under the Apache License, Version 2.0 (the \"License\");\n * you may not use this file except in compliance with the License.\n * You may obtain a copy of the License at\n *\n *     http://www.apache.org/licenses/LICENSE-2.0\n *\n * Unless required by applicable law or agreed to in writing, software\n * distributed under the License is distributed on an \"AS IS\" BASIS,\n * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n * See the License for the specific language governing permissions and\n * limitations under the License.\n */\n\n/**\n * A Marker Clusterer that clusters markers.\n *\n * @param {google.maps.Map} map The Google map to attach to.\n * @param {Array.<google.maps.Marker>=} opt_markers Optional markers to add to\n *   the cluster.\n * @param {Object=} opt_options support the following options:\n *     'gridSize': (number) The grid size of a cluster in pixels.\n *     'maxZoom': (number) The maximum zoom level that a marker can be part of a\n *                cluster.\n *     'zoomOnClick': (boolean) Whether the default behaviour of clicking on a\n *                    cluster is to zoom into it.\n *     'averageCenter': (boolean) Whether the center of each cluster should be\n *                      the average of all markers in the cluster.\n *     'minimumClusterSize': (number) The minimum number of markers to be in a\n *                           cluster before the markers are hidden and a count\n *                           is shown.\n *     'styles': (object) An object that has style properties:\n *       'url': (string) The image url.\n *       'height': (number) The image height.\n *       'width': (number) The image width.\n *       'anchor': (Array) The anchor position of the label text.\n *       'textColor': (string) The text color.\n *       'textSize': (number) The text size.\n *       'backgroundPosition': (string) The position of the backgound x, y.\n * @constructor\n * @extends google.maps.OverlayView\n */\nfunction MarkerClusterer(map, opt_markers, opt_options) {\n  // MarkerClusterer implements google.maps.OverlayView interface. We use the\n  // extend function to extend MarkerClusterer with google.maps.OverlayView\n  // because it might not always be available when the code is defined so we\n  // look for it at the last possible moment. If it doesn't exist now then\n  // there is no point going ahead :)\n  this.extend(MarkerClusterer, google.maps.OverlayView);\n  this.map_ = map;\n\n  /**\n   * @type {Array.<google.maps.Marker>}\n   * @private\n   */\n  this.markers_ = [];\n\n  /**\n   *  @type {Array.<Cluster>}\n   */\n  this.clusters_ = [];\n\n  this.sizes = [53, 56, 66, 78, 90];\n\n  /**\n   * @private\n   */\n  this.styles_ = [];\n\n  /**\n   * @type {boolean}\n   * @private\n   */\n  this.ready_ = false;\n\n  var options = opt_options || {};\n\n  /**\n   * @type {number}\n   * @private\n   */\n  this.gridSize_ = options['gridSize'] || 60;\n\n  /**\n   * @private\n   */\n  this.minClusterSize_ = options['minimumClusterSize'] || 4;\n\n  /**\n   * @type {?number}\n   * @private\n   */\n  this.maxZoom_ = options['maxZoom'] || null;\n\n  this.styles_ = options['styles'] || [];\n\n  /**\n   * @type {string}\n   * @private\n   */\n  this.imagePath_ = options['imagePath'] || this.MARKER_CLUSTER_IMAGE_PATH_;\n\n  /**\n   * @type {string}\n   * @private\n   */\n  this.imageExtension_ = options['imageExtension'] || this.MARKER_CLUSTER_IMAGE_EXTENSION_;\n\n  /**\n   * @type {boolean}\n   * @private\n   */\n  this.zoomOnClick_ = true;\n\n  if (options['zoomOnClick'] != undefined) {\n    this.zoomOnClick_ = options['zoomOnClick'];\n  }\n\n  /**\n   * @type {boolean}\n   * @private\n   */\n  this.averageCenter_ = false;\n\n  if (options['averageCenter'] != undefined) {\n    this.averageCenter_ = options['averageCenter'];\n  }\n\n  this.setupStyles_();\n\n  this.setMap(map);\n\n  /**\n   * @type {number}\n   * @private\n   */\n  this.prevZoom_ = this.map_.getZoom();\n\n  // Add the map event listeners\n  var that = this;\n  google.maps.event.addListener(this.map_, 'zoom_changed', function () {\n    // Determines map type and prevent illegal zoom levels\n    var zoom = that.map_.getZoom();\n    var minZoom = that.map_.minZoom || 0;\n    var maxZoom = Math.min(that.map_.maxZoom || 100, that.map_.mapTypes[that.map_.getMapTypeId()].maxZoom);\n    zoom = Math.min(Math.max(zoom, minZoom), maxZoom);\n\n    if (that.prevZoom_ != zoom) {\n      that.prevZoom_ = zoom;\n      that.resetViewport();\n    }\n  });\n\n  google.maps.event.addListener(this.map_, 'idle', function () {\n    that.redraw();\n  });\n\n  // Finally, add the markers\n  if (opt_markers && (opt_markers.length || Object.keys(opt_markers).length)) {\n    this.addMarkers(opt_markers, false);\n  }\n}\n\n/**\n * The marker cluster image path.\n *\n * @type {string}\n * @private\n */\nMarkerClusterer.prototype.MARKER_CLUSTER_IMAGE_PATH_ = '../images/m';\n\n/**\n * The marker cluster image path.\n *\n * @type {string}\n * @private\n */\nMarkerClusterer.prototype.MARKER_CLUSTER_IMAGE_EXTENSION_ = 'png';\n\n/**\n * Extends a objects prototype by anothers.\n *\n * @param {Object} obj1 The object to be extended.\n * @param {Object} obj2 The object to extend with.\n * @return {Object} The new extended object.\n * @ignore\n */\nMarkerClusterer.prototype.extend = function (obj1, obj2) {\n  return function (object) {\n    for (var property in object.prototype) {\n      this.prototype[property] = object.prototype[property];\n    }\n    return this;\n  }.apply(obj1, [obj2]);\n};\n\n/**\n * Implementaion of the interface method.\n * @ignore\n */\nMarkerClusterer.prototype.onAdd = function () {\n  this.setReady_(true);\n};\n\n/**\n * Implementaion of the interface method.\n * @ignore\n */\nMarkerClusterer.prototype.draw = function () {};\n\n/**\n * Sets up the styles object.\n *\n * @private\n */\nMarkerClusterer.prototype.setupStyles_ = function () {\n  if (this.styles_.length) {\n    return;\n  }\n\n  for (var i = 0, size; size = this.sizes[i]; i++) {\n    this.styles_.push({\n      url: this.imagePath_ + (i + 1) + '.' + this.imageExtension_,\n      height: size,\n      width: size\n    });\n  }\n};\n\n/**\n *  Fit the map to the bounds of the markers in the clusterer.\n */\nMarkerClusterer.prototype.fitMapToMarkers = function () {\n  var markers = this.getMarkers();\n  var bounds = new google.maps.LatLngBounds();\n  for (var i = 0, marker; marker = markers[i]; i++) {\n    bounds.extend(marker.getPosition());\n  }\n\n  this.map_.fitBounds(bounds);\n};\n\n/**\n *  Sets the styles.\n *\n *  @param {Object} styles The style to set.\n */\nMarkerClusterer.prototype.setStyles = function (styles) {\n  this.styles_ = styles;\n};\n\n/**\n *  Gets the styles.\n *\n *  @return {Object} The styles object.\n */\nMarkerClusterer.prototype.getStyles = function () {\n  return this.styles_;\n};\n\n/**\n * Whether zoom on click is set.\n *\n * @return {boolean} True if zoomOnClick_ is set.\n */\nMarkerClusterer.prototype.isZoomOnClick = function () {\n  return this.zoomOnClick_;\n};\n\n/**\n * Whether average center is set.\n *\n * @return {boolean} True if averageCenter_ is set.\n */\nMarkerClusterer.prototype.isAverageCenter = function () {\n  return this.averageCenter_;\n};\n\n/**\n *  Returns the array of markers in the clusterer.\n *\n *  @return {Array.<google.maps.Marker>} The markers.\n */\nMarkerClusterer.prototype.getMarkers = function () {\n  return this.markers_;\n};\n\n/**\n *  Returns the number of markers in the clusterer\n *\n *  @return {Number} The number of markers.\n */\nMarkerClusterer.prototype.getTotalMarkers = function () {\n  return this.markers_.length;\n};\n\n/**\n *  Sets the max zoom for the clusterer.\n *\n *  @param {number} maxZoom The max zoom level.\n */\nMarkerClusterer.prototype.setMaxZoom = function (maxZoom) {\n  this.maxZoom_ = maxZoom;\n};\n\n/**\n *  Gets the max zoom for the clusterer.\n *\n *  @return {number} The max zoom level.\n */\nMarkerClusterer.prototype.getMaxZoom = function () {\n  return this.maxZoom_;\n};\n\n/**\n *  The function for calculating the cluster icon image.\n *\n *  @param {Array.<google.maps.Marker>} markers The markers in the clusterer.\n *  @param {number} numStyles The number of styles available.\n *  @return {Object} A object properties: 'text' (string) and 'index' (number).\n *  @private\n */\nMarkerClusterer.prototype.calculator_ = function (markers, numStyles) {\n  var index = 0;\n  var count = markers.length;\n  var dv = count;\n  while (dv !== 0) {\n    dv = parseInt(dv / 10, 10);\n    index++;\n  }\n\n  index = Math.min(index, numStyles);\n  return {\n    text: count,\n    index: index\n  };\n};\n\n/**\n * Set the calculator function.\n *\n * @param {function(Array, number)} calculator The function to set as the\n *     calculator. The function should return a object properties:\n *     'text' (string) and 'index' (number).\n *\n */\nMarkerClusterer.prototype.setCalculator = function (calculator) {\n  this.calculator_ = calculator;\n};\n\n/**\n * Get the calculator function.\n *\n * @return {function(Array, number)} the calculator function.\n */\nMarkerClusterer.prototype.getCalculator = function () {\n  return this.calculator_;\n};\n\n/**\n * Add an array of markers to the clusterer.\n *\n * @param {Array.<google.maps.Marker>} markers The markers to add.\n * @param {boolean=} opt_nodraw Whether to redraw the clusters.\n */\nMarkerClusterer.prototype.addMarkers = function (markers, opt_nodraw) {\n  if (markers.length) {\n    for (var i = 0, marker; marker = markers[i]; i++) {\n      this.pushMarkerTo_(marker);\n    }\n  } else if (Object.keys(markers).length) {\n    for (var marker in markers) {\n      this.pushMarkerTo_(markers[marker]);\n    }\n  }\n  if (!opt_nodraw) {\n    this.redraw();\n  }\n};\n\n/**\n * Pushes a marker to the clusterer.\n *\n * @param {google.maps.Marker} marker The marker to add.\n * @private\n */\nMarkerClusterer.prototype.pushMarkerTo_ = function (marker) {\n  marker.isAdded = false;\n  if (marker['draggable']) {\n    // If the marker is draggable add a listener so we update the clusters on\n    // the drag end.\n    var that = this;\n    google.maps.event.addListener(marker, 'dragend', function () {\n      marker.isAdded = false;\n      that.repaint();\n    });\n  }\n  this.markers_.push(marker);\n};\n\n/**\n * Adds a marker to the clusterer and redraws if needed.\n *\n * @param {google.maps.Marker} marker The marker to add.\n * @param {boolean=} opt_nodraw Whether to redraw the clusters.\n */\nMarkerClusterer.prototype.addMarker = function (marker, opt_nodraw) {\n  this.pushMarkerTo_(marker);\n  if (!opt_nodraw) {\n    this.redraw();\n  }\n};\n\n/**\n * Removes a marker and returns true if removed, false if not\n *\n * @param {google.maps.Marker} marker The marker to remove\n * @return {boolean} Whether the marker was removed or not\n * @private\n */\nMarkerClusterer.prototype.removeMarker_ = function (marker) {\n  var index = -1;\n  if (this.markers_.indexOf) {\n    index = this.markers_.indexOf(marker);\n  } else {\n    for (var i = 0, m; m = this.markers_[i]; i++) {\n      if (m == marker) {\n        index = i;\n        break;\n      }\n    }\n  }\n\n  if (index == -1) {\n    // Marker is not in our list of markers.\n    return false;\n  }\n\n  marker.setMap(null);\n\n  this.markers_.splice(index, 1);\n\n  return true;\n};\n\n/**\n * Remove a marker from the cluster.\n *\n * @param {google.maps.Marker} marker The marker to remove.\n * @param {boolean=} opt_nodraw Optional boolean to force no redraw.\n * @return {boolean} True if the marker was removed.\n */\nMarkerClusterer.prototype.removeMarker = function (marker, opt_nodraw) {\n  var removed = this.removeMarker_(marker);\n\n  if (!opt_nodraw && removed) {\n    this.resetViewport();\n    this.redraw();\n    return true;\n  } else {\n    return false;\n  }\n};\n\n/**\n * Removes an array of markers from the cluster.\n *\n * @param {Array.<google.maps.Marker>} markers The markers to remove.\n * @param {boolean=} opt_nodraw Optional boolean to force no redraw.\n */\nMarkerClusterer.prototype.removeMarkers = function (markers, opt_nodraw) {\n  var removed = false;\n\n  for (var i = 0, marker; marker = markers[i]; i++) {\n    var r = this.removeMarker_(marker);\n    removed = removed || r;\n  }\n\n  if (!opt_nodraw && removed) {\n    this.resetViewport();\n    this.redraw();\n    return true;\n  }\n};\n\n/**\n * Sets the clusterer's ready state.\n *\n * @param {boolean} ready The state.\n * @private\n */\nMarkerClusterer.prototype.setReady_ = function (ready) {\n  if (!this.ready_) {\n    this.ready_ = ready;\n    this.createClusters_();\n  }\n};\n\n/**\n * Returns the number of clusters in the clusterer.\n *\n * @return {number} The number of clusters.\n */\nMarkerClusterer.prototype.getTotalClusters = function () {\n  return this.clusters_.length;\n};\n\n/**\n * Returns the google map that the clusterer is associated with.\n *\n * @return {google.maps.Map} The map.\n */\nMarkerClusterer.prototype.getMap = function () {\n  return this.map_;\n};\n\n/**\n * Sets the google map that the clusterer is associated with.\n *\n * @param {google.maps.Map} map The map.\n */\nMarkerClusterer.prototype.setMap = function (map) {\n  this.map_ = map;\n};\n\n/**\n * Returns the size of the grid.\n *\n * @return {number} The grid size.\n */\nMarkerClusterer.prototype.getGridSize = function () {\n  return this.gridSize_;\n};\n\n/**\n * Sets the size of the grid.\n *\n * @param {number} size The grid size.\n */\nMarkerClusterer.prototype.setGridSize = function (size) {\n  this.gridSize_ = size;\n};\n\n/**\n * Returns the min cluster size.\n *\n * @return {number} The grid size.\n */\nMarkerClusterer.prototype.getMinClusterSize = function () {\n  return this.minClusterSize_;\n};\n\n/**\n * Sets the min cluster size.\n *\n * @param {number} size The grid size.\n */\nMarkerClusterer.prototype.setMinClusterSize = function (size) {\n  this.minClusterSize_ = size;\n};\n\n/**\n * Extends a bounds object by the grid size.\n *\n * @param {google.maps.LatLngBounds} bounds The bounds to extend.\n * @return {google.maps.LatLngBounds} The extended bounds.\n */\nMarkerClusterer.prototype.getExtendedBounds = function (bounds) {\n  var projection = this.getProjection();\n\n  // Turn the bounds into latlng.\n  var tr = new google.maps.LatLng(bounds.getNorthEast().lat(), bounds.getNorthEast().lng());\n  var bl = new google.maps.LatLng(bounds.getSouthWest().lat(), bounds.getSouthWest().lng());\n\n  // Convert the points to pixels and the extend out by the grid size.\n  var trPix = projection.fromLatLngToDivPixel(tr);\n  trPix.x += this.gridSize_;\n  trPix.y -= this.gridSize_;\n\n  var blPix = projection.fromLatLngToDivPixel(bl);\n  blPix.x -= this.gridSize_;\n  blPix.y += this.gridSize_;\n\n  // Convert the pixel points back to LatLng\n  var ne = projection.fromDivPixelToLatLng(trPix);\n  var sw = projection.fromDivPixelToLatLng(blPix);\n\n  // Extend the bounds to contain the new bounds.\n  bounds.extend(ne);\n  bounds.extend(sw);\n\n  return bounds;\n};\n\n/**\n * Determins if a marker is contained in a bounds.\n *\n * @param {google.maps.Marker} marker The marker to check.\n * @param {google.maps.LatLngBounds} bounds The bounds to check against.\n * @return {boolean} True if the marker is in the bounds.\n * @private\n */\nMarkerClusterer.prototype.isMarkerInBounds_ = function (marker, bounds) {\n  return bounds.contains(marker.getPosition());\n};\n\n/**\n * Clears all clusters and markers from the clusterer.\n */\nMarkerClusterer.prototype.clearMarkers = function () {\n  this.resetViewport(true);\n\n  // Set the markers a empty array.\n  this.markers_ = [];\n};\n\n/**\n * Clears all existing clusters and recreates them.\n * @param {boolean} opt_hide To also hide the marker.\n */\nMarkerClusterer.prototype.resetViewport = function (opt_hide) {\n  // Remove all the clusters\n  for (var i = 0, cluster; cluster = this.clusters_[i]; i++) {\n    cluster.remove();\n  }\n\n  // Reset the markers to not be added and to be invisible.\n  for (var i = 0, marker; marker = this.markers_[i]; i++) {\n    marker.isAdded = false;\n    if (opt_hide) {\n      marker.setMap(null);\n    }\n  }\n\n  this.clusters_ = [];\n};\n\n/**\n *\n */\nMarkerClusterer.prototype.repaint = function () {\n  var oldClusters = this.clusters_.slice();\n  this.clusters_.length = 0;\n  this.resetViewport();\n  this.redraw();\n\n  // Remove the old clusters.\n  // Do it in a timeout so the other clusters have been drawn first.\n  window.setTimeout(function () {\n    for (var i = 0, cluster; cluster = oldClusters[i]; i++) {\n      cluster.remove();\n    }\n  }, 0);\n};\n\n/**\n * Redraws the clusters.\n */\nMarkerClusterer.prototype.redraw = function () {\n  this.createClusters_();\n};\n\n/**\n * Calculates the distance between two latlng locations in km.\n * @see http://www.movable-type.co.uk/scripts/latlong.html\n *\n * @param {google.maps.LatLng} p1 The first lat lng point.\n * @param {google.maps.LatLng} p2 The second lat lng point.\n * @return {number} The distance between the two points in km.\n * @private\n*/\nMarkerClusterer.prototype.distanceBetweenPoints_ = function (p1, p2) {\n  if (!p1 || !p2) {\n    return 0;\n  }\n\n  var R = 6371; // Radius of the Earth in km\n  var dLat = (p2.lat() - p1.lat()) * Math.PI / 180;\n  var dLon = (p2.lng() - p1.lng()) * Math.PI / 180;\n  var a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(p1.lat() * Math.PI / 180) * Math.cos(p2.lat() * Math.PI / 180) * Math.sin(dLon / 2) * Math.sin(dLon / 2);\n  var c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));\n  var d = R * c;\n  return d;\n};\n\n/**\n * Add a marker to a cluster, or creates a new cluster.\n *\n * @param {google.maps.Marker} marker The marker to add.\n * @private\n */\nMarkerClusterer.prototype.addToClosestCluster_ = function (marker) {\n  var distance = 40000; // Some large number\n  var clusterToAddTo = null;\n  var pos = marker.getPosition();\n  for (var i = 0, cluster; cluster = this.clusters_[i]; i++) {\n    var center = cluster.getCenter();\n    if (center) {\n      var d = this.distanceBetweenPoints_(center, marker.getPosition());\n      if (d < distance) {\n        distance = d;\n        clusterToAddTo = cluster;\n      }\n    }\n  }\n\n  if (clusterToAddTo && clusterToAddTo.isMarkerInClusterBounds(marker)) {\n    clusterToAddTo.addMarker(marker);\n  } else {\n    var cluster = new Cluster(this);\n    cluster.addMarker(marker);\n    this.clusters_.push(cluster);\n  }\n};\n\n/**\n * Creates the clusters.\n *\n * @private\n */\nMarkerClusterer.prototype.createClusters_ = function () {\n  if (!this.ready_) {\n    return;\n  }\n\n  // Get our current map view bounds.\n  // Create a new bounds object so we don't affect the map.\n  var mapBounds = new google.maps.LatLngBounds(this.map_.getBounds().getSouthWest(), this.map_.getBounds().getNorthEast());\n  var bounds = this.getExtendedBounds(mapBounds);\n\n  for (var i = 0, marker; marker = this.markers_[i]; i++) {\n    if (!marker.isAdded && this.isMarkerInBounds_(marker, bounds)) {\n      this.addToClosestCluster_(marker);\n    }\n  }\n};\n\n/**\n * A cluster that contains markers.\n *\n * @param {MarkerClusterer} markerClusterer The markerclusterer that this\n *     cluster is associated with.\n * @constructor\n * @ignore\n */\nfunction Cluster(markerClusterer) {\n  this.markerClusterer_ = markerClusterer;\n  this.map_ = markerClusterer.getMap();\n  this.gridSize_ = markerClusterer.getGridSize();\n  this.minClusterSize_ = markerClusterer.getMinClusterSize();\n  this.averageCenter_ = markerClusterer.isAverageCenter();\n  this.center_ = null;\n  this.markers_ = [];\n  this.bounds_ = null;\n  this.clusterIcon_ = new ClusterIcon(this, markerClusterer.getStyles(), markerClusterer.getGridSize());\n}\n\n/**\n * Determins if a marker is already added to the cluster.\n *\n * @param {google.maps.Marker} marker The marker to check.\n * @return {boolean} True if the marker is already added.\n */\nCluster.prototype.isMarkerAlreadyAdded = function (marker) {\n  if (this.markers_.indexOf) {\n    return this.markers_.indexOf(marker) != -1;\n  } else {\n    for (var i = 0, m; m = this.markers_[i]; i++) {\n      if (m == marker) {\n        return true;\n      }\n    }\n  }\n  return false;\n};\n\n/**\n * Add a marker the cluster.\n *\n * @param {google.maps.Marker} marker The marker to add.\n * @return {boolean} True if the marker was added.\n */\nCluster.prototype.addMarker = function (marker) {\n  if (this.isMarkerAlreadyAdded(marker)) {\n    return false;\n  }\n\n  if (!this.center_) {\n    this.center_ = marker.getPosition();\n    this.calculateBounds_();\n  } else {\n    if (this.averageCenter_) {\n      var l = this.markers_.length + 1;\n      var lat = (this.center_.lat() * (l - 1) + marker.getPosition().lat()) / l;\n      var lng = (this.center_.lng() * (l - 1) + marker.getPosition().lng()) / l;\n      this.center_ = new google.maps.LatLng(lat, lng);\n      this.calculateBounds_();\n    }\n  }\n\n  marker.isAdded = true;\n  this.markers_.push(marker);\n\n  var len = this.markers_.length;\n  if (len < this.minClusterSize_ && marker.getMap() != this.map_) {\n    // Min cluster size not reached so show the marker.\n    marker.setMap(this.map_);\n  }\n\n  if (len == this.minClusterSize_) {\n    // Hide the markers that were showing.\n    for (var i = 0; i < len; i++) {\n      this.markers_[i].setMap(null);\n    }\n  }\n\n  if (len >= this.minClusterSize_) {\n    marker.setMap(null);\n  }\n\n  this.updateIcon();\n  return true;\n};\n\n/**\n * Returns the marker clusterer that the cluster is associated with.\n *\n * @return {MarkerClusterer} The associated marker clusterer.\n */\nCluster.prototype.getMarkerClusterer = function () {\n  return this.markerClusterer_;\n};\n\n/**\n * Returns the bounds of the cluster.\n *\n * @return {google.maps.LatLngBounds} the cluster bounds.\n */\nCluster.prototype.getBounds = function () {\n  var bounds = new google.maps.LatLngBounds(this.center_, this.center_);\n  var markers = this.getMarkers();\n  for (var i = 0, marker; marker = markers[i]; i++) {\n    bounds.extend(marker.getPosition());\n  }\n  return bounds;\n};\n\n/**\n * Removes the cluster\n */\nCluster.prototype.remove = function () {\n  this.clusterIcon_.remove();\n  this.markers_.length = 0;\n  delete this.markers_;\n};\n\n/**\n * Returns the center of the cluster.\n *\n * @return {number} The cluster center.\n */\nCluster.prototype.getSize = function () {\n  return this.markers_.length;\n};\n\n/**\n * Returns the center of the cluster.\n *\n * @return {Array.<google.maps.Marker>} The cluster center.\n */\nCluster.prototype.getMarkers = function () {\n  return this.markers_;\n};\n\n/**\n * Returns the center of the cluster.\n *\n * @return {google.maps.LatLng} The cluster center.\n */\nCluster.prototype.getCenter = function () {\n  return this.center_;\n};\n\n/**\n * Calculated the extended bounds of the cluster with the grid.\n *\n * @private\n */\nCluster.prototype.calculateBounds_ = function () {\n  var bounds = new google.maps.LatLngBounds(this.center_, this.center_);\n  this.bounds_ = this.markerClusterer_.getExtendedBounds(bounds);\n};\n\n/**\n * Determines if a marker lies in the clusters bounds.\n *\n * @param {google.maps.Marker} marker The marker to check.\n * @return {boolean} True if the marker lies in the bounds.\n */\nCluster.prototype.isMarkerInClusterBounds = function (marker) {\n  return this.bounds_.contains(marker.getPosition());\n};\n\n/**\n * Returns the map that the cluster is associated with.\n *\n * @return {google.maps.Map} The map.\n */\nCluster.prototype.getMap = function () {\n  return this.map_;\n};\n\n/**\n * Updates the cluster icon\n */\nCluster.prototype.updateIcon = function () {\n  var zoom = this.map_.getZoom();\n  var mz = this.markerClusterer_.getMaxZoom();\n\n  if (mz && zoom > mz) {\n    // The zoom is greater than our max zoom so show all the markers in cluster.\n    for (var i = 0, marker; marker = this.markers_[i]; i++) {\n      marker.setMap(this.map_);\n    }\n    return;\n  }\n\n  if (this.markers_.length < this.minClusterSize_) {\n    // Min cluster size not yet reached.\n    this.clusterIcon_.hide();\n    return;\n  }\n\n  var numStyles = this.markerClusterer_.getStyles().length;\n  var sums = this.markerClusterer_.getCalculator()(this.markers_, numStyles);\n  this.clusterIcon_.setCenter(this.center_);\n  this.clusterIcon_.setSums(sums);\n  this.clusterIcon_.show();\n};\n\n/**\n * A cluster icon\n *\n * @param {Cluster} cluster The cluster to be associated with.\n * @param {Object} styles An object that has style properties:\n *     'url': (string) The image url.\n *     'height': (number) The image height.\n *     'width': (number) The image width.\n *     'anchor': (Array) The anchor position of the label text.\n *     'textColor': (string) The text color.\n *     'textSize': (number) The text size.\n *     'backgroundPosition: (string) The background postition x, y.\n * @param {number=} opt_padding Optional padding to apply to the cluster icon.\n * @constructor\n * @extends google.maps.OverlayView\n * @ignore\n */\nfunction ClusterIcon(cluster, styles, opt_padding) {\n  cluster.getMarkerClusterer().extend(ClusterIcon, google.maps.OverlayView);\n\n  this.styles_ = styles;\n  this.padding_ = opt_padding || 0;\n  this.cluster_ = cluster;\n  this.center_ = null;\n  this.map_ = cluster.getMap();\n  this.div_ = null;\n  this.sums_ = null;\n  this.visible_ = false;\n\n  this.setMap(this.map_);\n}\n\n/**\n * Triggers the clusterclick event and zoom's if the option is set.\n */\nClusterIcon.prototype.triggerClusterClick = function () {\n  var markerClusterer = this.cluster_.getMarkerClusterer();\n\n  // Trigger the clusterclick event.\n  google.maps.event.trigger(markerClusterer, 'clusterclick', this.cluster_);\n\n  if (markerClusterer.isZoomOnClick()) {\n    // Zoom into the cluster.\n    this.map_.fitBounds(this.cluster_.getBounds());\n  }\n};\n\n/**\n * Adding the cluster icon to the dom.\n * @ignore\n */\nClusterIcon.prototype.onAdd = function () {\n  this.div_ = document.createElement('DIV');\n  if (this.visible_) {\n    var pos = this.getPosFromLatLng_(this.center_);\n    this.div_.style.cssText = this.createCss(pos);\n    this.div_.innerHTML = this.sums_.text;\n  }\n\n  var panes = this.getPanes();\n  panes.overlayMouseTarget.appendChild(this.div_);\n\n  var that = this;\n  google.maps.event.addDomListener(this.div_, 'click', function () {\n    that.triggerClusterClick();\n  });\n};\n\n/**\n * Returns the position to place the div dending on the latlng.\n *\n * @param {google.maps.LatLng} latlng The position in latlng.\n * @return {google.maps.Point} The position in pixels.\n * @private\n */\nClusterIcon.prototype.getPosFromLatLng_ = function (latlng) {\n  var pos = this.getProjection().fromLatLngToDivPixel(latlng);\n  pos.x -= parseInt(this.width_ / 2, 10);\n  pos.y -= parseInt(this.height_ / 2, 10);\n  return pos;\n};\n\n/**\n * Draw the icon.\n * @ignore\n */\nClusterIcon.prototype.draw = function () {\n  if (this.visible_) {\n    var pos = this.getPosFromLatLng_(this.center_);\n    this.div_.style.top = pos.y + 'px';\n    this.div_.style.left = pos.x + 'px';\n  }\n};\n\n/**\n * Hide the icon.\n */\nClusterIcon.prototype.hide = function () {\n  if (this.div_) {\n    this.div_.style.display = 'none';\n  }\n  this.visible_ = false;\n};\n\n/**\n * Position and show the icon.\n */\nClusterIcon.prototype.show = function () {\n  if (this.div_) {\n    var pos = this.getPosFromLatLng_(this.center_);\n    this.div_.style.cssText = this.createCss(pos);\n    this.div_.style.display = '';\n  }\n  this.visible_ = true;\n};\n\n/**\n * Remove the icon from the map\n */\nClusterIcon.prototype.remove = function () {\n  this.setMap(null);\n};\n\n/**\n * Implementation of the onRemove interface.\n * @ignore\n */\nClusterIcon.prototype.onRemove = function () {\n  if (this.div_ && this.div_.parentNode) {\n    this.hide();\n    this.div_.parentNode.removeChild(this.div_);\n    this.div_ = null;\n  }\n};\n\n/**\n * Set the sums of the icon.\n *\n * @param {Object} sums The sums containing:\n *   'text': (string) The text to display in the icon.\n *   'index': (number) The style index of the icon.\n */\nClusterIcon.prototype.setSums = function (sums) {\n  this.sums_ = sums;\n  this.text_ = sums.text;\n  this.index_ = sums.index;\n  if (this.div_) {\n    this.div_.innerHTML = sums.text;\n  }\n\n  this.useStyle();\n};\n\n/**\n * Sets the icon to the the styles.\n */\nClusterIcon.prototype.useStyle = function () {\n  var index = Math.max(0, this.sums_.index - 1);\n  index = Math.min(this.styles_.length - 1, index);\n  var style = this.styles_[index];\n  this.url_ = style['url'];\n  this.height_ = style['height'];\n  this.width_ = style['width'];\n  this.textColor_ = style['textColor'];\n  this.anchor_ = style['anchor'];\n  this.textSize_ = style['textSize'];\n  this.backgroundPosition_ = style['backgroundPosition'];\n};\n\n/**\n * Sets the center of the icon.\n *\n * @param {google.maps.LatLng} center The latlng to set as the center.\n */\nClusterIcon.prototype.setCenter = function (center) {\n  this.center_ = center;\n};\n\n/**\n * Create the css text based on the position of the icon.\n *\n * @param {google.maps.Point} pos The position.\n * @return {string} The css style text.\n */\nClusterIcon.prototype.createCss = function (pos) {\n  var style = [];\n  style.push('background-image:url(' + this.url_ + ');');\n  var backgroundPosition = this.backgroundPosition_ ? this.backgroundPosition_ : '0 0';\n  style.push('background-position:' + backgroundPosition + ';');\n\n  if (_typeof(this.anchor_) === 'object') {\n    if (typeof this.anchor_[0] === 'number' && this.anchor_[0] > 0 && this.anchor_[0] < this.height_) {\n      style.push('height:' + (this.height_ - this.anchor_[0]) + 'px; padding-top:' + this.anchor_[0] + 'px;');\n    } else {\n      style.push('height:' + this.height_ + 'px; line-height:' + this.height_ + 'px;');\n    }\n    if (typeof this.anchor_[1] === 'number' && this.anchor_[1] > 0 && this.anchor_[1] < this.width_) {\n      style.push('width:' + (this.width_ - this.anchor_[1]) + 'px; padding-left:' + this.anchor_[1] + 'px;');\n    } else {\n      style.push('width:' + this.width_ + 'px; text-align:center;');\n    }\n  } else {\n    style.push('height:' + this.height_ + 'px; line-height:' + this.height_ + 'px; width:' + this.width_ + 'px; text-align:center;');\n  }\n\n  var txtColor = this.textColor_ ? this.textColor_ : 'black';\n  var txtSize = this.textSize_ ? this.textSize_ : 11;\n\n  style.push('cursor:pointer; top:' + pos.y + 'px; left:' + pos.x + 'px; color:' + txtColor + '; position:absolute; font-size:' + txtSize + 'px; font-family:Arial,sans-serif; font-weight:bold');\n  return style.join('');\n};\n\nmodule.exports = {\n  MarkerClusterer: MarkerClusterer,\n  Cluster: Cluster,\n  ClusterIcon: ClusterIcon\n};\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/utils/gmap-markercluster.js\n// module id = 356\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/utils/gmap-markercluster.js?")},357:function(module,exports,__webpack_require__){"use strict";eval("\n\n/*\n * This demo illustrates the coordinate system used to display map tiles in the\n * API.\n *\n * Tiles in Google Maps are numbered from the same origin as that for\n * pixels. For Google's implementation of the Mercator projection, the origin\n * tile is always at the northwest corner of the map, with x values increasing\n * from west to east and y values increasing from north to south.\n *\n * Try panning and zooming the map to see how the coordinates change.\n */\n\n/** @constructor */\nfunction TileMarkerMapType(map, tileMarkerFun) {\n    this.map = map;\n    this.mapMoving = false;\n    this.tileMarkerFun = tileMarkerFun;\n    this.tileSizePx = 256;\n    //    this.tileSizeAddjustment = 1;\n    this.tileSize = new google.maps.Size(this.tileSizePx, this.tileSizePx);\n    this.tileMarkersCompleted = {};\n    this.debug = false;\n\n    var _this = this;\n    //    window.setTimeout(function(){ _this.mapMoving = false; }, 6000);\n    map.addListener('drag', function () {\n        _this.mapMoving = true;\n    }); // check this too because we are waiting a little before seting off\n    map.addListener('dragstart', function () {\n        _this.mapMoving = true;\n    });\n    // wait a little before seting off\n    //map.addListener('dragend', function() { window.setTimeout( function() { _this.mapMoving = false; } , 1000 ) });\n    map.addListener('idle', function () {\n        window.setTimeout(function () {\n            _this.mapMoving = false;\n        }, 1000);\n    });\n}\n\nTileMarkerMapType.prototype.getTile = function (coord, zoom, ownerDocument) {\n    //    console.log('coord:', coord);\n    var c2 = this.coordAdjMaxZoom(coord, zoom);\n    //    console.log('c2:', c2);\n    var latLng = this.coord2LatLng(c2.coord, c2.zoom);\n    //    console.log('latLng:', latLng);\n\n    var tile = ownerDocument.createElement('div');\n    tile.style.width = this.tileSize.width + 'px';\n    tile.style.height = this.tileSize.height + 'px';\n    tile.setAttribute('data-tile-visible', 'true');\n\n    if (this.debug) {\n        tile.innerHTML = '(' + c2.zoom + ': ' + c2.coord.x + ',' + c2.coord.y + ')';\n        tile.style.fontSize = '10';\n        tile.style.borderStyle = 'solid';\n        tile.style.borderWidth = '1px';\n        tile.style.borderColor = '#AAAAAA';\n    }\n\n    this.waitForVisibleTile(tile, latLng);\n\n    return tile;\n};\n\nTileMarkerMapType.prototype.releaseTile = function (tile) {\n    tile.setAttribute('data-tile-visible', 'false');\n};\n\nTileMarkerMapType.prototype.waitForVisibleTile = function (tile, latLng) {\n\n    var _this = this;\n    var thisCallback = function thisCallback() {\n        // check _this the tile is visible\n        var tileVisible = tile.getAttribute('data-tile-visible');\n        //        console.log('tileVisible: ',tileVisible);\n        if (tileVisible == 'true') {\n            // wait for movement to stop\n            if (_this.isMapMoving()) {\n                window.setTimeout(thisCallback, 500);\n            } else {\n                var tileMarkerKey = latLng.center.lat + '-' + latLng.center.lng + '-' + latLng.radiusMiles;\n                if (_this.tileMarkersCompleted[tileMarkerKey] !== true) {\n                    // prevent it from creating markers again for the same tile\n                    _this.tileMarkersCompleted[tileMarkerKey] = true;\n                    _this.tileMarkerFun(latLng.center, latLng.radiusMiles, latLng.NW, latLng.SE);\n                }\n            }\n        }\n    };\n\n    thisCallback();\n};\n\nTileMarkerMapType.prototype.isMapMoving = function () {\n    return this.mapMoving;\n};\n\n//:::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::\n//:::                                                                         :::\n//:::  This routine calculates the distance between two points (given the     :::\n//:::  latitude/longitude of those points). It is being used to calculate     :::\n//:::  the distance between two locations using GeoDataSource (TM) prodducts  :::\n//:::                                                                         :::\n//:::  Definitions:                                                           :::\n//:::    South latitudes are negative, east longitudes are positive           :::\n//:::                                                                         :::\n//:::  Passed to function:                                                    :::\n//:::    lat1, lng1 = Latitude and Longitude of point 1 (in decimal degrees)  :::\n//:::    lat2, lng2 = Latitude and Longitude of point 2 (in decimal degrees)  :::\n//:::    unit = the unit you desire for results                               :::\n//:::           where: 'M' is statute miles (default)                         :::\n//:::                  'K' is kilometers                                      :::\n//:::                  'N' is nautical miles                                  :::\n//:::                                                                         :::\n//:::  Worldwide cities and other features databases with latitude longitude  :::\n//:::  are available at http://www.geodatasource.com                          :::\n//:::                                                                         :::\n//:::  For enquiries, please contact sales@geodatasource.com                  :::\n//:::                                                                         :::\n//:::  Official Web site: http://www.geodatasource.com                        :::\n//:::                                                                         :::\n//:::               GeoDataSource.com (C) All Rights Reserved 2015            :::\n//:::                                                                         :::\n//:::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::\n\n\nTileMarkerMapType.prototype.distance = function distance(lat1, lng1, lat2, lng2, unit) {\n    var radlat1 = Math.PI * lat1 / 180;\n    var radlat2 = Math.PI * lat2 / 180;\n    var theta = lng1 - lng2;\n    var radtheta = Math.PI * theta / 180;\n    var dist = Math.sin(radlat1) * Math.sin(radlat2) + Math.cos(radlat1) * Math.cos(radlat2) * Math.cos(radtheta);\n    dist = Math.acos(dist);\n    dist = dist * 180 / Math.PI;\n    dist = dist * 60 * 1.1515;\n    if (unit == \"K\") {\n        dist = dist * 1.609344;\n    }\n    if (unit == \"N\") {\n        dist = dist * 0.8684;\n    }\n    return dist;\n};\n\nTileMarkerMapType.prototype.tile2lat = function (y, z) {\n    var n = Math.PI - 2 * Math.PI * y / Math.pow(2, z);\n    return 180 / Math.PI * Math.atan(0.5 * (Math.exp(n) - Math.exp(-n)));\n};\nTileMarkerMapType.prototype.tile2lng = function (x, z) {\n    return x / Math.pow(2, z) * 360 - 180;\n};\n\nTileMarkerMapType.prototype.coordAdjMaxZoom = function (coord, zoom) {\n    var x = coord.x;\n    var y = coord.y;\n    var z = zoom;\n\n    if (zoom > 14) {\n        z = 14;\n        var coordAdjust = Math.pow(2, zoom - 14);\n        x = Math.floor(x / coordAdjust);\n        y = Math.floor(y / coordAdjust);\n        //console.log('coordAdjust:', coordAdjust);\n    }\n    return { coord: { x: x, y: y }, zoom: z };\n};\nTileMarkerMapType.prototype.coord2LatLng = function (coord, zoom) {\n    var latLng = {\n        NW: {\n            lat: this.tile2lat(coord.y, zoom),\n            lng: this.tile2lng(coord.x, zoom)\n        },\n        SE: {\n            lat: this.tile2lat(coord.y + 1, zoom),\n            lng: this.tile2lng(coord.x + 1, zoom)\n        },\n        center: {\n            lat: this.tile2lat(coord.y + 0.5, zoom),\n            lng: this.tile2lng(coord.x + 0.5, zoom)\n        }\n    };\n    latLng.radiusMiles = this.distance(latLng.NW.lat, latLng.NW.lng, latLng.center.lat, latLng.center.lng);\n    return latLng;\n};\n\nTileMarkerMapType.prototype.setDebug = function (debug) {\n    this.debug = debug;\n};\n\nmodule.exports = {\n    TileMarkerMapType: TileMarkerMapType\n};\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/utils/gmap-tilemarker.js\n// module id = 357\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/utils/gmap-tilemarker.js?")},547:function(module,exports){eval("// removed by extract-text-webpack-plugin\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scss/components/pulsemarker.scss\n// module id = 547\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scss/components/pulsemarker.scss?")},552:function(module,exports){eval("// removed by extract-text-webpack-plugin\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scss/pages/beerlocator.scss\n// module id = 552\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scss/pages/beerlocator.scss?")},569:function(module,exports,__webpack_require__){eval('var H = __webpack_require__(27);\nmodule.exports = function() { var T = new H.Template({code: function (c,p,i) { var t=this;t.b(i=i||"");t.b("<div class=\\"pop_up_info\\">\\r");t.b("\\n" + i);t.b("  <h3 class=\\"caps_large\\"><strong>");t.b(t.v(t.f("Name",c,p,0)));t.b("</strong></h3>\\r");t.b("\\n" + i);t.b("  <div class=\\"address\\">");t.b(t.v(t.f("Address",c,p,0)));t.b("<br/> ");t.b(t.v(t.f("City",c,p,0)));t.b(", ");t.b(t.v(t.f("State",c,p,0)));t.b(" ");t.b(t.v(t.f("ZipCode",c,p,0)));t.b("</div>\\r");t.b("\\n" + i);t.b("  <div class=\\"distance\\">");t.b(t.v(t.f("Distance",c,p,0)));t.b(" miles / <a href=\\"https://maps.google.com?saddr=Current+Location&amp;daddr=");t.b(t.v(t.f("Latitude",c,p,0)));t.b(",");t.b(t.v(t.f("Longitude",c,p,0)));t.b("\\" target=\\"_blank\\">Get directions</a></div>\\r");t.b("\\n" + i);t.b("  <br/>\\r");t.b("\\n" + i);t.b("    <div class=\\"products\\">\\r");t.b("\\n" + i);t.b("      Available Products:\\r");t.b("\\n" + i);if(t.s(t.f("DosBrands",c,p,1),c,p,0,428,451,"{{ }}")){t.rs(c,p,function(c,p,t){t.b("        ");t.b(t.v(t.d(".",c,p,0)));t.b("\\r");t.b("\\n" + i);});c.pop();}t.b("    </div>\\r");t.b("\\n" + i);t.b("</div>\\r");t.b("\\n");return t.fl(); },partials: {}, subs: {  }}, "<div class=\\"pop_up_info\\">\\r\\n  <h3 class=\\"caps_large\\"><strong>{{Name}}</strong></h3>\\r\\n  <div class=\\"address\\">{{Address}}<br/> {{City}}, {{State}} {{ZipCode}}</div>\\r\\n  <div class=\\"distance\\">{{Distance}} miles / <a href=\\"https://maps.google.com?saddr=Current+Location&amp;daddr={{Latitude}},{{Longitude}}\\" target=\\"_blank\\">Get directions</a></div>\\r\\n  <br/>\\r\\n    <div class=\\"products\\">\\r\\n      Available Products:\\r\\n      {{#DosBrands}}\\r\\n        {{.}}\\r\\n      {{/DosBrands}}\\r\\n    </div>\\r\\n</div>\\r\\n", H);return T.render.apply(T, arguments); };\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/beerlocator/templates/info-window.html\n// module id = 569\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/beerlocator/templates/info-window.html?')},570:function(module,exports,__webpack_require__){eval('var H = __webpack_require__(27);\nmodule.exports = function() { var T = new H.Template({code: function (c,p,i) { var t=this;t.b(i=i||"");t.b("<div id=\\"");t.b(t.v(t.f("id",c,p,0)));t.b("\\" data-id=\\"");t.b(t.v(t.f("id",c,p,0)));t.b("\\" class=\\"bar\\">\\r");t.b("\\n" + i);t.b("  <div class=\\"content-max-width center\\"><div class=\\"col location\\">\\r");t.b("\\n" + i);t.b("    \x3c!-- <div>");t.b(t.v(t.f("id",c,p,0)));t.b("</div> --\x3e\\r");t.b("\\n" + i);t.b("      <div class=\\"colLft\\">\\r");t.b("\\n" + i);t.b("        <img src=\\"/Assets/Images/beerlocator/pin.png\\"></div><div class=\\"colRgt\\">\\r");t.b("\\n" + i);t.b("          <p class=\\"name\\">");t.b(t.v(t.f("Name",c,p,0)));t.b("</p>\\r");t.b("\\n" + i);t.b("          <p>");t.b(t.v(t.f("Address",c,p,0)));t.b(" ");t.b(t.v(t.f("City",c,p,0)));t.b(",");t.b(t.v(t.f("State",c,p,0)));t.b(" ");t.b(t.v(t.f("ZipCode",c,p,0)));t.b("</p>\\r");t.b("\\n" + i);t.b("          <a data-ga=\\"{&quot;category&quot;:&quot;list&quot;,&quot;label&quot;:&quot;get directions-");t.b(t.v(t.f("Name",c,p,0)));t.b("&quot;}\\" class=\\"button\\" href=\\"https://maps.google.com?saddr=Current+Location&amp;daddr=");t.b(t.v(t.f("Latitude",c,p,0)));t.b(",");t.b(t.v(t.f("Longitude",c,p,0)));t.b("\\" target=\\"_blank\\">Get directions</a>\\r");t.b("\\n" + i);t.b("        </div>\\r");t.b("\\n" + i);t.b("      </div>\\r");t.b("\\n" + i);t.b("      <div class=\\"col brands\\">\\r");t.b("\\n" + i);t.b("        Available Products:<br/>\\r");t.b("\\n" + i);if(t.s(t.f("DosBrands",c,p,1),c,p,0,736,763,"{{ }}")){t.rs(c,p,function(c,p,t){t.b("          ");t.b(t.v(t.d(".",c,p,0)));t.b("\\r");t.b("\\n" + i);});c.pop();}t.b("      </div>\\r");t.b("\\n" + i);t.b("      <div class=\\"col distance\\">\\r");t.b("\\n" + i);t.b("        <p>");t.b(t.v(t.f("Distance",c,p,0)));t.b(" miles</p>\\r");t.b("\\n" + i);t.b("        </div>\\r");t.b("\\n" + i);t.b("   </div>\\r");t.b("\\n" + i);t.b("</div>\\r");t.b("\\n" + i);t.b("\x3c!-- <div>dist from center = ");t.b(t.v(t.f("total_distance",c,p,0)));t.b("</div> --\x3e\\r");t.b("\\n");return t.fl(); },partials: {}, subs: {  }}, "<div id=\\"{{id}}\\" data-id=\\"{{id}}\\" class=\\"bar\\">\\r\\n  <div class=\\"content-max-width center\\"><div class=\\"col location\\">\\r\\n    \x3c!-- <div>{{id}}</div> --\x3e\\r\\n      <div class=\\"colLft\\">\\r\\n        <img src=\\"/Assets/Images/beerlocator/pin.png\\"></div><div class=\\"colRgt\\">\\r\\n          <p class=\\"name\\">{{Name}}</p>\\r\\n          <p>{{Address}} {{City}},{{State}} {{ZipCode}}</p>\\r\\n          <a data-ga=\\"{&quot;category&quot;:&quot;list&quot;,&quot;label&quot;:&quot;get directions-{{Name}}&quot;}\\" class=\\"button\\" href=\\"https://maps.google.com?saddr=Current+Location&amp;daddr={{Latitude}},{{Longitude}}\\" target=\\"_blank\\">Get directions</a>\\r\\n        </div>\\r\\n      </div>\\r\\n      <div class=\\"col brands\\">\\r\\n        Available Products:<br/>\\r\\n        {{#DosBrands}}\\r\\n          {{.}}\\r\\n        {{/DosBrands}}\\r\\n      </div>\\r\\n      <div class=\\"col distance\\">\\r\\n        <p>{{Distance}} miles</p>\\r\\n        </div>\\r\\n   </div>\\r\\n</div>\\r\\n\x3c!-- <div>dist from center = {{total_distance}}</div> --\x3e\\r\\n", H);return T.render.apply(T, arguments); };\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/beerlocator/templates/list-item.html\n// module id = 570\n// module chunks = 5\n\n//# sourceURL=webpack:///./dev/scripts/beerlocator/templates/list-item.html?')},574:function(module,exports){eval("// shim for using process in browser\nvar process = module.exports = {};\n\n// cached from whatever global is present so that test runners that stub it\n// don't break things.  But we need to wrap it in a try catch in case it is\n// wrapped in strict mode code which doesn't define any globals.  It's inside a\n// function because try/catches deoptimize in certain engines.\n\nvar cachedSetTimeout;\nvar cachedClearTimeout;\n\nfunction defaultSetTimout() {\n    throw new Error('setTimeout has not been defined');\n}\nfunction defaultClearTimeout () {\n    throw new Error('clearTimeout has not been defined');\n}\n(function () {\n    try {\n        if (typeof setTimeout === 'function') {\n            cachedSetTimeout = setTimeout;\n        } else {\n            cachedSetTimeout = defaultSetTimout;\n        }\n    } catch (e) {\n        cachedSetTimeout = defaultSetTimout;\n    }\n    try {\n        if (typeof clearTimeout === 'function') {\n            cachedClearTimeout = clearTimeout;\n        } else {\n            cachedClearTimeout = defaultClearTimeout;\n        }\n    } catch (e) {\n        cachedClearTimeout = defaultClearTimeout;\n    }\n} ())\nfunction runTimeout(fun) {\n    if (cachedSetTimeout === setTimeout) {\n        //normal enviroments in sane situations\n        return setTimeout(fun, 0);\n    }\n    // if setTimeout wasn't available but was latter defined\n    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {\n        cachedSetTimeout = setTimeout;\n        return setTimeout(fun, 0);\n    }\n    try {\n        // when when somebody has screwed with setTimeout but no I.E. maddness\n        return cachedSetTimeout(fun, 0);\n    } catch(e){\n        try {\n            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally\n            return cachedSetTimeout.call(null, fun, 0);\n        } catch(e){\n            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error\n            return cachedSetTimeout.call(this, fun, 0);\n        }\n    }\n\n\n}\nfunction runClearTimeout(marker) {\n    if (cachedClearTimeout === clearTimeout) {\n        //normal enviroments in sane situations\n        return clearTimeout(marker);\n    }\n    // if clearTimeout wasn't available but was latter defined\n    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {\n        cachedClearTimeout = clearTimeout;\n        return clearTimeout(marker);\n    }\n    try {\n        // when when somebody has screwed with setTimeout but no I.E. maddness\n        return cachedClearTimeout(marker);\n    } catch (e){\n        try {\n            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally\n            return cachedClearTimeout.call(null, marker);\n        } catch (e){\n            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.\n            // Some versions of I.E. have different rules for clearTimeout vs setTimeout\n            return cachedClearTimeout.call(this, marker);\n        }\n    }\n\n\n\n}\nvar queue = [];\nvar draining = false;\nvar currentQueue;\nvar queueIndex = -1;\n\nfunction cleanUpNextTick() {\n    if (!draining || !currentQueue) {\n        return;\n    }\n    draining = false;\n    if (currentQueue.length) {\n        queue = currentQueue.concat(queue);\n    } else {\n        queueIndex = -1;\n    }\n    if (queue.length) {\n        drainQueue();\n    }\n}\n\nfunction drainQueue() {\n    if (draining) {\n        return;\n    }\n    var timeout = runTimeout(cleanUpNextTick);\n    draining = true;\n\n    var len = queue.length;\n    while(len) {\n        currentQueue = queue;\n        queue = [];\n        while (++queueIndex < len) {\n            if (currentQueue) {\n                currentQueue[queueIndex].run();\n            }\n        }\n        queueIndex = -1;\n        len = queue.length;\n    }\n    currentQueue = null;\n    draining = false;\n    runClearTimeout(timeout);\n}\n\nprocess.nextTick = function (fun) {\n    var args = new Array(arguments.length - 1);\n    if (arguments.length > 1) {\n        for (var i = 1; i < arguments.length; i++) {\n            args[i - 1] = arguments[i];\n        }\n    }\n    queue.push(new Item(fun, args));\n    if (queue.length === 1 && !draining) {\n        runTimeout(drainQueue);\n    }\n};\n\n// v8 likes predictible objects\nfunction Item(fun, array) {\n    this.fun = fun;\n    this.array = array;\n}\nItem.prototype.run = function () {\n    this.fun.apply(null, this.array);\n};\nprocess.title = 'browser';\nprocess.browser = true;\nprocess.env = {};\nprocess.argv = [];\nprocess.version = ''; // empty string to avoid regexp issues\nprocess.versions = {};\n\nfunction noop() {}\n\nprocess.on = noop;\nprocess.addListener = noop;\nprocess.once = noop;\nprocess.off = noop;\nprocess.removeListener = noop;\nprocess.removeAllListeners = noop;\nprocess.emit = noop;\nprocess.prependListener = noop;\nprocess.prependOnceListener = noop;\n\nprocess.listeners = function (name) { return [] }\n\nprocess.binding = function (name) {\n    throw new Error('process.binding is not supported');\n};\n\nprocess.cwd = function () { return '/' };\nprocess.chdir = function (dir) {\n    throw new Error('process.chdir is not supported');\n};\nprocess.umask = function() { return 0; };\n\n\n//////////////////\n// WEBPACK FOOTER\n// ./~/node-libs-browser/~/process/browser.js\n// module id = 574\n// module chunks = 5\n\n//# sourceURL=webpack:///./~/node-libs-browser/~/process/browser.js?")},578:function(module,exports){eval("/* (ignored) */\n\n//////////////////\n// WEBPACK FOOTER\n// vertx (ignored)\n// module id = 578\n// module chunks = 5\n\n//# sourceURL=webpack:///vertx_(ignored)?")},583:function(module,exports,__webpack_require__){eval("module.exports = __webpack_require__(319);\n\n\n//////////////////\n// WEBPACK FOOTER\n// multi ./dev/scripts/beerlocator/beerlocator\n// module id = 583\n// module chunks = 5\n\n//# sourceURL=webpack:///multi_./dev/scripts/beerlocator/beerlocator?")},72:function(module,exports,__webpack_require__){"use strict";eval("/* WEBPACK VAR INJECTION */(function($) {\n\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\n__webpack_require__(117);\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\nvar template = __webpack_require__(126);\n__webpack_require__(63);\nvar imagesrc = __webpack_require__(127);\n\nvar Loader = function () {\n    function Loader(selector) {\n        _classCallCheck(this, Loader);\n\n        this.$el = selector;\n        this.render();\n    }\n\n    _createClass(Loader, [{\n        key: 'render',\n        value: function render() {\n            this.div = $('<div class=\"site-loader\">');\n            var d = {\n                img: imagesrc\n            };\n            var html = $(template(d)).prependTo(this.div);\n            $(this.$el).append(this.div);\n        }\n    }, {\n        key: 'show',\n        value: function show(parent) {\n            this.div.transition({ scale: 0.5, opacity: 1 });\n        }\n    }, {\n        key: 'hide',\n        value: function hide(parent) {\n            var self = this;\n            // $(self.wrapper).fadeOut(function () {\n            //     self.wrapper.css('z-index', -1)\n            //     if (parent) self.wrapper.parent(parent).remove();\n            // });\n            this.div.transition({\n                opacity: 0, scale: 0,\n                duration: 500,\n                easing: 'in',\n                complete: function complete() {\n                    if (parent) self.wrapper.parent(parent).remove();\n                }\n            });\n        }\n    }]);\n\n    return Loader;\n}();\n\nexports.default = Loader;\n/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(1)))\n\n//////////////////\n// WEBPACK FOOTER\n// ./dev/scripts/components/loader/loader.js\n// module id = 72\n// module chunks = 2 3 5\n\n//# sourceURL=webpack:///./dev/scripts/components/loader/loader.js?")}},[583]);